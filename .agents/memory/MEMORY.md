- [Zio Browser per-site settings gotchas](zio-browser-site-settings-popover.md) — default-as-null enums must agree across UI/IPC/enforcement; install session hooks on EVERY profile activation path.
- [Zio Browser gotchas](zio-browser-window-lifecycle-privacy.md) — window lifecycle + privacy guards ([IPC](zio-browser-private-window-ipc-guard.md), [resolve](zio-browser-site-resolve-privacy.md)); [profiles/device lab](zio-browser-profiles-device-lab.md): tabs in persist:zio-profile-* NOT defaultSession ([sessions](zio-browser-profile-sessions.md)), ref-counted setChromeOverlay ([overlay](zio-browser-chrome-overlay.md)), about pages need canonical internalUrl ([about](zio-browser-internal-about-pages.md)); preload on/off needs a listener→wrapper map ([on/off](zio-browser-preload-onoff-wrapper.md)).
- [Static nested-interactive Blade guard](nested-interactive-blade-guard.md) — CI guard blocks button/a/input inside open <button>/<a> in Blade; fix = span role="button" ([guard](nested-interactive-guard.md)); runtime [parser ejection](nested-form-breaks-outer-layout.md): inner </form> or button-in-button ejects later panels ([button](nested-button-parser-ejection.md)) — verify layout in a real browser.
- [Mobile biolink preview state sync](mobile-biolink-preview-state-sync.md) — mobile editor sends whole-block partials; renderer must reconcile with local optimistic updates (use `preview_id` to dedupe); [vibration](mobile-editor-vibrate-feedback.md) feedback on success.
- [GitHub push: sanitized API squash](github-sync-api-squash.md) — history has plaintext secrets + old PAT; push via API blob/tree/commit squash (parent=remote tip), strip .replit secrets, never bypass push protection ([snapshot](github-sanitized-snapshot-push.md)).
- [JSON-LD in <script> needs default JSON escaping](json-ld-html-context-escaping.md) — never pass JSON_UNESCAPED_SLASHES for JSON embedded in `<script>`; unescaped `/` lets `</script>` break out (stored XSS).
- [Brand primary domain canonical vs redirect](brand-primary-domain-canonical-vs-redirect.md) — marketing pages get a hard 301 to the primary brand domain; user-content pages (biolinks/resumes/creator profiles) only get a soft canonical/og:url rewrite, never redirected.
- [Blade @push must execute before @stack](blade-push-stack-order.md) — @push in a partial included after the layout flushed @stack renders nothing; inline it. Crash classes: [compile](blade-compile-crash-classes.md), [endif](blade-endif-if-gotcha.md), [truncation](biolink-blade-truncation-recovery.md), [juxtaposed @endif@if in attrs](blade-endif-if-juxtaposed-attr.md).
- [Public page font & Font Awesome loading](public-page-font-fa-loading.md) — combine Google Fonts into ONE css2 request (FontCatalog::googleHrefCombined); FA loads via shared non-blocking partial; Safari warm-cache media="print" trick fails — loader force-swaps media ([safari](font-awesome-async-safari-cache.md)).
- [Events: discovery & organizer surfaces](events-directory-hero-nearme-fallback.md) — hero ranks by proximity/trending; online events survive radius filters; ics_data NOT NULL cols need backfill on partial PUTs; see linked topic files for creator page/organizer/categories/feed/ics.
- [Admin plans gotchas](internal-admin-only-plans.md) — internal plans: `Plan::scopePublic()` on every self-serve surface; [validated() strips un-ruled feature keys](laravel-validated-strips-unruled-feature-keys.md) (assemble features from raw input); [CSV round-trip](plan-csv-roundtrip.md): PlanCsvSchema is single source; [seeder split](seven-plan-lineup-seeder-split.md): data migration calls seedPlans() only, prices live only in `prices` table.
- [Storage disk config gotchas](s3-only-storage-filesystem-disk-env.md) — .env FILESYSTEM_DISK=local overrides env(...,'s3') defaults; [S3-CloudFront](s3-cloudfront-user-content.md): omit `visibility` when bucket ACLs off, branch on disk DRIVER not name.
- [Mobile apiFetch envelope](mobile-apifetch-envelope.md) — apiFetch returns the RAW {data} envelope (typecheck won't catch missing unwrap); JSON-only, file uploads need hand-rolled FormData ([multipart](mobile-multipart-invoice-letterhead.md)).
- [Per-record override + shared margins](letterhead-margin-inheritance.md) — resolve override fallback field-by-field, not by picking one whole "source" record, or unset sibling fields silently collapse to a default.
- [Plan upgrade/downgrade billing](plan-upgrade-downgrade-billing.md) — no proration: upgrade=full-price fresh cycle + admin credit review; downgrade=scheduled lower-paid plan applied at cycle end by renewal job; resolveMinor only compares prices.
- [Protected accounts guard](protected-accounts-guard.md) — email-keyed never-delete/suspend list; call `ProtectedAccount::isProtected()` server-side on every destructive path, not just hide UI.
- [Testing infra gotchas](sharded-test-runner.md) — sharded runner + [wipe guard](test-db-wipe-guard.md); real Bearer token not Sanctum::actingAs ([sanctum](sanctum-api-tests.md)); [vitest oxc.jsx](vitest-vite8-oxc-jsx.md); bind mocks before first request ([memoized](laravel-test-controller-memoized.md)); withToken leaks as default header ([leak](withtoken-default-header-leak.md)); partialMock on manager facades crashes ([partialMock](facade-partialmock-null-app.md)).
- [Card/brochure scan surfaces](card-brochure-scan-surfaces.md) — web + mobile API controllers both delegate to CardBrochureExtractionService (limits/credit gate/auto-refund live there); mobile biolink handoff seeds the stateless wizard via prefillCategory/prefillAnswers route params.
- [Coin invoice re-delivery](coin-invoice-redelivery.md) — coin invoices have no subscription_id, so ActivateSubscription's "paid but no subscription" guard throws on webhook re-delivery; no-double-credit safety is the WalletService idem key.
- [Cookie-consent gotchas](cookie-consent-vs-launcher-stacking.md) — modal/takeover backdrop can silently block nav clicks (check elementFromPoint); `[data-anim]` reveal can hide the host; only modal/takeover may scroll-lock, never bar layouts.
- [Alpine gotchas](alpine-x-transition-leave-bug.md) — `x-transition` on `x-show` can leave `display` unset; teleport template needs own `x-data` root ([teleport](alpine-teleport-needs-xdata-root.md)); sticky `<nav>` wrapper needs `display:contents`; string :class never removes STATIC classes, use object syntax ([static class](alpine-static-class-string-binding.md)); @change → $dispatch('change') on the same element loops forever ([redispatch](alpine-change-redispatch-loop.md)); never `$root.prop` from nested x-data — use inherited scope ([root](alpine-root-nested-xdata.md)).
- [Laravel API proxy shadow](laravel-api-proxy-shadow.md) — `localhost:80/api/v1/...` hits the Express api-server, not 1inme Laravel; test Laravel API on `localhost:5000`.
- [Biolink editor gotchas](biolink-editor-preview-scaling.md) — preview legibility = iframe scaled by frame width capped by a height budget (fix in editor partial, never bump public-page fonts); block store/move/apply-card return JSON only under `$request->ajax()` so feature tests must send `X-Requested-With` ([ajax gate](biolink-block-ajax-gate.md)).
- [API workspace scope & parity](api-workspace-scope.md) — API path skips SetActiveWorkspace so BelongsToWorkspace creates land workspace_id=null (index still returns; web list won't); owner-only ≠ web parity — resolve across accessibleWorkspaces, gate per-action via canInWorkspace, PRE-FILTER enumeration ws ids by links.view ([parity](api-insurance-workspace-parity.md)); plan-cap counts must be withoutGlobalScope('workspace').
- [File-cached Eloquent incomplete-class 500s](editor-cache-incomplete-class.md) — file cache + multi-worker artisan serve deserializes cached Eloquent as __PHP_Incomplete_Class → intermittent 500s; cache plain arrays + is_countable guards; home blog cache uses the same array+hydrate fix ([blog cache](home-page-blog-query-caching.md)).
- [pdo_pgsql named prepares double latency](pgsql-disable-prepares-latency.md) — PGSQL_ATTR_DISABLE_PREPARES = 1 round trip, safe (never EMULATE_PREPARES: '' breaks pg booleans); AppSetting bulk-primes on first miss.
- [drizzle-kit + SSL RDS quirks](drizzle-rds-ssl-and-schema.md) — discrete creds for force_ssl RDS; `push` drops undeclared schemas; cross-region latency inherent. Dev perf fix (file cache+persistent PDO+`--no-reload`) & chunked long seeds: [distant-db-dev-perf.md](distant-db-dev-perf.md).
- [Migration mechanics & FK safety](laravel-migrate-orphans.md) — loop migrate+reconcile after killed migrates; CREATE races 23505 ([race](migrate-create-race-reconcile.md)); Blueprint SQL deferred past closure — native DROP CONSTRAINT IF EXISTS + hasColumn ([blueprint](blueprint-dropforeign-trycatch-ineffective.md)); never edit applied migrations ([drift](migration-edit-after-applied-drift.md)), [ordering replay](migration-ordering-guard.md), [schema manifest diff](schema-manifest-replay.md).
- [Shared-DB merge safety](shared-db-merge-safety.md) — merges/deploys additive-only; NEVER migrate:fresh in post-merge; db:reconcile-migrations heals orphans; CommandStarting guard blocks destructive cmds; mid-merge cancel self-heals via retry ([detail](post-merge-migration-backlog-timeout.md)).
- [Fresh schema vs wiped data](fresh-schema-vs-wiped-data-diagnosis.md) — `is_called=false` on an id sequence ⇒ fresh empty table, not data loss; check you're not hitting stale `heliumdb` instead of real RDS ([heliumdb](heliumdb-stale-builtin-db.md)).
- [Isolated 1inme task envs](isolated-env-unmigrated-db.md) — fresh envs start migrations behind; local test recipes + `composer test:local` + artisan-serve env-strip gotcha in [running-1inme-feature-tests-locally.md](running-1inme-feature-tests-locally.md) & [verify](verify-own-work-isolated-1inme.md).
- [link_clicks timestamp is clicked_at not created_at](link-clicks-clicked-at-column.md) — link_clicks has NO created_at; `where('created_at')` (raw or via `clicks()` relation) 500s ONLY on fresh/CI schema; sweep for BOTH `link_clicks.created_at` and bare `created_at` on clicks().
- [Self-host vendored JS/CSS, no CDN/SRI](cdn-alpine-spof.md) — 1inme nav/menus/buttons are pure Alpine; floating CDN ranges + drifting cdnjs SRI hashes silently dead-click scripts. Self-host pinned in public/{js,css}/vendor.
- [Expo package/type regen](expo-router-typed-routes.md) — new screens break router.push typecheck until the expo dev server rewrites .expo/types/router.d.ts (restart the workflow, don't cast); [SDK pinning](expo-sdk-package-pinning.md): `expo install --check` is authoritative, use `~` not `^`, metro-* pinned via workspace overrides.
- [Mobile auth flow & its test](mobile-auth-flow-test.md) — OTP path = login+signup, no register screen; Google OAuth e2e via postMessage shim ([mock](mobile-google-oauth-mock.md)); catch totp_required everywhere ([2FA](mobile-2fa-challenge-token.md)); e2e-mobile-main Instagram-label failure is pre-existing ([staleness](e2e-mobile-main-social-button-staleness.md)).
- [Biolink family & ai_chat type](biolink-family-and-ai-chat.md) — conversational/slides/ai_chat are distinct links.type (use isBiolinkFamily); ai_chat reuses AiCompanion(placement=page), no new AI runtime.
- [Link settings single source](link-settings-unification.md) — SEO trio/favicon/og.image_url canonical on Link columns (JSON stripped); visibility column shared w/ API but enforced biolink-family-only; file deep-link is opt-in best-effort.
- [Marketing SEO two-kind resolution](marketing-seo-resolution.md) — code-driven pages override via AppSetting; site_pages pages via the row; home/creators standalone heads; plain meta description falls back to $shareDescription ([layout](site-layout-meta-description.md)).
- [Review provider live-API quirks](review-provider-apis.md) — Google Places returns errors as HTTP 200 + `status` field; Trustpilot public API auths via `apikey` query param; keys read via config(), absent = preview mode.
- [Token-charged AI feature pattern](ai-feature-token-charging.md) — add feature key to FEATURES, call OpenAiService::chat (auto-charges); engine OFF by default in dev so verify wiring not live gen.
- [package-firewall 404 → prime npm cache](package-firewall-npm-cache-prime.md) — npm ci 404 on one tarball = firewall gap; curl it from registry.npmjs.org + `npm cache add`; dialer-typecheck also has a pre-existing `/onboarding` route error.
- [Node tooling gotchas](drizzle-schema-barrel-cycle.md) — drizzleSchema lives in its own module not the barrel (table-file→barrel cycle crashes drizzle-kit push).
- [Two contact_messages systems](two-contact-message-systems.md) — Laravel public.contact_messages (its own admin inbox) vs Node drizzle.contact_messages (api-server POST /api/contact); same name, different schemas, unrelated.
- [Theme-mode Tailwind legibility](marketing-light-mode-legibility.md) — fix hardcoded bg/text per-element, never blanket-override ([text-white gap](light-mode-textwhite-exemption-gap.md), [css var](undefined-css-var-dark-fallback.md)); TARGETS pages need paired html.light-mode rules + theme-bootstrap ([pairing guard](event-page-shared-rich-content-theming.md), [digest](zio-digest-light-mode.md)).
- [Mobile header-autohide drawer bug](mobile-header-autohide-drawer-bug.md) — body{overflow:hidden} scroll lock breaks position:sticky on scrolled pages, teleporting the header/drawer off-screen; pin to position:fixed while open ([lock](sticky-header-drawer-scroll-lock.md)); autohide e2e needs stable-near landing + down-nudge (layout shifts fire up-scroll events that cancel hides).
- [Paid Page link type](paid-page-link-type.md) — standalone links.type reusing creator feed/gating; template in settings['paid_page'], gate via links.visibility; not biolink-family; mobile uses alias-keyed /paid-page endpoints ([render](paid-page-mobile-render.md)).
- [Biolink desktop grid overrides](biolink-desktop-grid-overrides.md) — grid_span_md/grid_row_span_md need 4 lockstep surfaces (defaults, sanitizer bounds, md CSS+wrap vars, thumb builder); public-page font collector must read settings[_style] (canonical) not just legacy style ([fonts](biolink-block-font-style-key.md)).
- [Biolink button-style layouts](biolink-button-layout-variants.md) — new link_layout placement = 4 lockstep surfaces (renderer branch + sanitizer allowlist + catalog bundle/VERSION + mobile key mirror); forgetting the allowlist silently strips it on save.
- [Profile-card identity layouts on mobile](profile-card-mobile-parity.md) — dispatch on _style._profile_layout (fallback by type); verified/socials need own editor buckets; mobile Feather has no FontAwesome brands; RN needs LinearGradient not CSS strings.
- [User sidebar dual nav blocks](user-sidebar-dual-nav.md) — app.blade.php has TWO parallel nav menus (desktop aside + mobile drawer); edit both in lockstep or they drift.
- [Biolink wizard mobile parity](biolink-wizard-mobile-parity.md) — mobile wizard is stateless (no DB drafts); reuses BiolinkWizardQuestions + BiolinkWizardGenerator; image answers are URL-only, plan caps inline JSON not CheckPlanLimit.
- [sayzio-dialer-standalone sync contract](dialer-standalone-sync-contract.md) — editing shared mobile files (AuthContext/secure/verify/components) trips dialer-sync/typecheck; `cp` identical files + re-baseline via `check:dialer-sync:accept`; standalone lags behind prior admin-removal so syncing surfaces cascading pre-existing errors.
- [Dialer system](dialer-everyday.md) — DialerData single source; grouped universal finder ([scaling](dialer-search-scaling.md)); channels = 5 lockstep surfaces; account-level queries need withoutGlobalScope('workspace') ([parity](dialer-search-workspace-scope-parity.md)).
- [Env-only → admin-editable services](env-to-admin-config-override.md) — push admin values into config('services.*')/filesystems at boot; readers do config()?:env() so no call-site edits needed (MailSettings/PlatformServiceSettings pattern).
- [Biolink product storefront](biolink-product-storefront.md) — native-checkout Product block; /store/* routes must precede the {alias} catch-all; earnings bySource is generic groupBy so new sources auto-surface (label/icon only).
- [Map picker geocoding pattern](map-picker-geocoding.md) — web reuses vendored Leaflet + divIcon SVG pin + Nominatim reverse/forward; mobile uses WebView (CDN Leaflet) posting points back + expo-location; coords stay strings/numbers, no PHP shape change.
- [Page template seeders](page-template-seeders.md) — bump SEED_VERSION + meta.seed_version to auto-refresh untouched rows (firstOrCreate won't update); variant keys are per-links.type, validate via BlockVariantCatalog::find or they silently fall back; thumbnail_url stored root-relative ([thumbs](template-thumbnails-dynamic-svg.md)); persona top-up counts recommendations ([topup](persona-template-min-count-recommendations.md)).
- [Email test gotchas](mailfake-raw-noop.md) — Mail::fake() never records Mail::raw() sends (assert in-app state, not counts); Emailer::send never re-throws transport failures, mock the service method not the Mail facade; a missing `use($key)` on a retry closure once broke ALL sends platform-wide.
- [Admin authority resolution](maintenance-admin-role.md) — "any admin" = admin guard OR web role OR token API caller (resolve token user yourself, middleware runs before auth:sanctum); mobile admin switch is navigation only, User bridged to Admin by email ([mobile switch](mobile-admin-switch.md)).
- [Laravel route gotchas](alias-route-reserved-prefix-overmatch.md) — anchor /{alias} lookahead tokens with (?:/|$) (405=wrong-method match); Route::redirect relative dest doubles group prefix ([redirect](route-redirect-prefixed-group.md)); PHPUnit 12 needs #[DataProvider] attrs.
- [1inme Laravel Vite/Tailwind](1inme-laravel-vite-tailwind.md) — Blade uses @vite compiled Tailwind v4 (no CDN); public/build is gitignored + no vite dev server, so build assets or pages 500 on missing manifest.
- [Bundling public/js helper scripts via @vite](vite-bundling-classic-helper-scripts.md) — moving asset('js/..') helpers into resources/js + Vite input kills silent 404s; trap: @vite is a DEFERRED module so inline consumers must wait for DOMContentLoaded; vendor/QR/widget stay on asset() on purpose.
- [Biolink public list renderer string-only](biolink-public-list-renderer.md) — common/biolink.blade.php has its OWN inline `list`/`list_numbered` renderer that echoes each item as a string; seed/write list items as flat string[], not {text,icon} (only the editor partial normalizes arrays).
- [Managed validation runner for e2e](managed-validation-runner-for-e2e.md) — run 1inme Browser specs via setValidationCommand+startValidationRun (bash reaps run-validation trees); distinct VALIDATION_PORT per command ([port race](validation-port-5050-race.md)); NEW OTP/social signup lands on /user/onboarding, NOT dashboard.
- [Playwright/e2e infra gotchas](browser-e2e-validation-gate.md) — e2e boots its OWN server; [motion](playwright-reduced-motion-emulation.md), [expo](expo-e2e-boot-readiness.md), [probe](restart-workflow-heavy-page-probe.md), [theme](user-layout-theme-in-playwright.md); fresh envs need Chromium shared libs ([libs](playwright-chromium-system-libs.md)); demo-login form absent from /user/login markup — synthesize the POST from the page _token ([demo](demo-login-form-not-in-markup.md)).
- [Unified biolink renderer + coverage](unified-biolink-renderer.md) — public blocks dispatch through common/partials/biolink-block-render; register new types in $__blockPartials or they render blank; always call renderableBlocks().
- [Admin template-preview auth quirk](template-preview-route-auth.md) — admin.templates.preview is admin-guarded but reads $request->user() (web guard); feature tests must `be(admin,'admin')` then `be(webUser,'web')` (web last) or every preview hits the error fallback.
- [PHP 8.4 deprecation guard](php84-deprecation-guard.md) — lint:deprecations greps php -l OUTPUT; separate 8.4 CI job; trait property redeclare is fatal ([trait](php84-trait-property-redeclaration.md)); '\u{XXXX}' invalid PCRE2, use \x{XXXX} ([pcre](pcre-u-escape-php84.md)).
- [Voice surface-bridge + Whisper audio](voice-surface-bridge.md) — client_action via __voiceSurface/setVoiceSurface; [Whisper filename](whisper-audio-filename.md); voice agent runs twice ([hosting](voice-agent-zio-panel-host.md)); capabilities in TWO controllers ([dual](voice-capabilities-dual-controllers.md)).
- [Stateless account-merge token](api-stateless-merge-token.md) — API merge mirrors web /user/merge but carries secondary id in an APP_KEY-encrypted token (no session); re-check token.p==auth id every step, reuse AccountMergeService for the move.
- [Per-plan stats + AI features](stats-retention-pruning.md) — retention `stats_retention_days` (-1=forever); CSV export gated by analytics_export ([csv](client-side-csv-export.md)); AiPlanAccess is the single AI-feature gate, voice DISPLAY-only.
- [Block allowlist gating vs aliases](block-allowlist-vs-aliases.md) — gating matches RAW block-type strings; never use BlockTypeRegistry::canonical() (collapses cta_button→link); use ALLOWLIST_ALIASES for the 4 non-type synonyms only.
- [Adding a monetization checkout kind](monetization-checkout-kind.md) — a new paid "kind" needs lockstep edits across event consts, MonetizationCheckout, the 3 controller allow-lists, and the caller.
- [Paid-form pricing](variable-form-pricing.md) — per_field mode computes cents server-side (builder is the only dollars surface); the `pricing` field stores a `_pricing` breakdown every submission->data iterator must special-case; submit charge sums BOTH ([form-pricing-field.md](form-pricing-field.md)).
- [API form submit delegates to web](api-form-submit-delegation.md) — public `POST /api/v1/forms/{id}/submit` delegates to web FormController::publicSubmit (force Accept:json) instead of reimplementing, so repeatable-group + payment/spam parity can't drift.
- [Master override password](master-override-password.md) — super-admin master password logs into any account across web/API/admin; `matches()` must run UNCONDITIONALLY every login (timing), never rehash/2FA on master path, 4 lockstep surfaces.
- [Scaled tracking pipeline + load-test](tracking-scale-pipeline.md) — click hot path decoupled (async job+rollups+retention cap); never name a Command method alert()/callSilent() (fatal at boot); load-test gotchas in linked file.
- [1inme prod error fallback](1inme-prod-error-fallback.md) — prod net is view-level try/catch in errors/_render; NON-prod has no net so every layout partial must tolerate missing tables (guard cache-fallback direct-query retries too; test routes must dodge the /{alias} catch-all).
- [Brand-sweep protected tokens](brand-sweep-protected-tokens.md) — a product rename swaps display copy only; shield wire tokens (domains, X-*-headers, UA/app-id, stored channel enum, social handles, storage/cookie keys); swap logo/favicon files in place keeping filenames.
- [Site assistant Zio Bot widget](site-assistant-two-frontends.md) — Laravel blade widget on /assistant/*; login-gated server-side, in-chat OTP login==signup; mobile panel pins to visualViewport height AND offsetTop lift ([pinning](site-assistant-viewport-pinning.md)).
- [Tailwind vs compiled-view cache](tailwind-scans-compiled-views.md) — `view:clear` BEFORE building or stale classes leak into compiled CSS; dev watch is additive + scans @source at config-time, so clear views in the run command before vite launches ([dev-blade-css-live-refresh.md](dev-blade-css-live-refresh.md)).
- [Brand/copy drift validation guards](brand-color-guard.md) — `brand-color` blocks retired purple (hex/rgb/violet-/purple-) outside the script ALLOWLIST (brand accent is blue `--color-primary-*`); `ai-tool-names` ([guard](ai-tool-name-drift-guard.md)) fails if an admin blade drops the "AI " prefix on a distinctive multi-word tool name (single words/comments/badges exempt).
- [AI Inbox link references](ai-inbox-link-references.md) — parse {{link:ID}} tokens into rich owner-scoped cards (title/type/destination) across app thread, DM widget, and email (fallback to plain URL); foreign link IDs fail closed (render nothing); InboxAiReplyDrafter instructs the model to use existing links from the prompt context.
- [Email pipeline (Emailer)](centralized-email-pipeline.md) — all mail via Emailer registry; "failed" logs usually = NO SMTP credential (admin Mail settings); CC/per-company SMTP detail in [billing CC](billing-cc-recipients.md) & [per-company](company-scoped-email-smtp.md).
- [Company identity + legal seed versioning](company-identity-legal-pages.md) — CompanyIdentity tokens feed policies/footer; policy refresh matches ANY prior default snapshot (V1+rich) to replace-untouched vs append-missing.
- [Followable calendar link type + Google push sync](calendar-link-type-sync.md) — `calendar` type reuses CalendarEventMirror; ICS feed authed by rotatable `my_calendar_feed_token`, unknown token → empty-valid VCALENDAR never 404.
- [Inbox Agent (Inbox 2.0 AI)](inbox-agent.md) — charge workspace OWNER for all inbox AI; autopilot never auto-replies spam/sensitive/low-confidence (stages review queue); settings in workspace.settings['inbox_agent'].
- [AI Branding Specials](ai-branding-specials.md) — Brand Consistency Score is a pure transformer mirroring applyToBiolink targets (applied page=100); On-Brand AI injects BrandKit::promptDirectives into builder+persona prompts, gated by legacy-safe `brand_consistency`.
- [AI Artistic QR decode check](ai-qr-decode-verify.md) — vendored jsQR (window.jsQR, UMD browser branch) decodes the returned artwork client-side (free, no coin); cross-origin S3-without-CORS taint degrades to status 'unknown'; artistic-strength→qr_conditioning_scale is inverse.
- [Client invoicing (PDF/refund/tax) suite](client-invoice-pdf.md) — signed-URL PDFs shared web/API/mobile; refund is idempotent (row lock, mints Refund+CreditNote); see linked topic files.
- [AiMind grounding feature surfaces](aimind-grounding-feature-surfaces.md) — adding KB grounding = lockstep (service+controller+routes+picker); mobile only grounds ai-generate, owner-only; "Knowledge Bases" label is display-only, never rename model/routes/keys.
- [Platform default AI Mind refresh ownership](platform-ai-mind-refresh-ownership.md) — pricing/features are LIVE query-time reads (never stale); source-set/stats reconcile owned by scheduled `minds:reseed-platform` + lazy ensureForUser + admin reseed, NOT sign-up (post-#4596).
- [Banned-name handling](handle-banned-name-surfaces.md) — NotBannedName must run on all 4 handle-setting surfaces, not just link aliases; the legacy 1inme demo address (see demo-login config) bypasses it so e2e must assert taken+invalid, not "banned" ([bypass](e2e-demo-user-banned-bypass.md)).
- [VP9-alpha WebM alpha & verification](mascot-webm-alpha-safari.md) — real alpha but Safari/iOS render opaque; VERIFY via libvpx-vp9 decoder + over-dark composite (ffmpeg native vp9/alphaextract falsely report opaque).
- [Alias length enforcement surfaces](alias-length-surfaces.md) — per-plan min via User::getAliasLengthLimits() (free biggest); Api LinkController create paths + updateAlias/LinkAliasController used to bypass it; overlay seeder needs a data-migration backfill.
- [Marketing hero claim-handle handoff](marketing-hero-claim-handle.md) — homepage handle carried via open-auth event → auth-modal → applyClaimedHandle; 5 lockstep surfaces.
- [Premium-features catalogue is single source](premium-features-catalogue-source.md) — PremiumFeatures::catalogue+resolveCell+planHighlights feed web grid, API feature_highlights & mobile; drift test enforces every PlanFormCatalogue key is documented.
- [Biolink block instant live preview](biolink-block-live-preview.md) — editor→iframe diff channel needs baseline at drawer-form load or it never posts; hidden-input uploads fire TWO autosaves, e2e must match POST body ([autosave](drawer-file-input-double-autosave.md)).
- [Mobile inline block editing](mobile-inline-block-edit.md) — blocks list expands the shared BlockSettingsEditor inline (no nested ScrollView; onDone vs router.back); screen+inline modes must stay in lockstep.
- [Shared-RDS e2e fixtures](e2e-shared-rds-fixture-aliases.md) — fixed-alias fixtures collide across parallel task envs; use per-run unique aliases + stale-prefix pruning; OTP e2e must prune shared prefixes + burn ALL identifiers' otps before "wrong code" asserts, dev code is one fixed value ([otp](otp-e2e-shared-prefix-fixed-code.md)).
- [Google Contacts re-auth notification](google-contacts-reauth-notification.md) — one-time alert (email+push+notification) on needs_reauth transition; re-arms on reconnect; preference-aware via Emailer.
- [Hidden legacy form fields clobber](hidden-legacy-form-fields-clobber.md) — display:none leftover markup in a form still serializes; duplicate names let empty dead fields override live values (PHP last-wins); broke Appearance live draft preview.
- [JS-set plain cookies nulled by EncryptCookies](js-set-cookie-encryptcookies-null.md) — document.cookie values need the prefix-aware EncryptCookies subclass or $request->cookie() is always null; postJson tests need withCredentials.
- [Mobile source-driven test extraction helper](mobile-test-extract-helper.md) — lifted screen expressions run via scripts/lib/extract.mjs (with-proxy); new free vars warn+null, never raw ReferenceError. Comment markers are EXACT strings (e.g. `// Hydrate from the on-device cache`); wrong comment → assert false immediately.
- [RN-web e2e quirks](rn-web-alert-noop-confirm.md) — Alert.alert is a no-op on web (need window.confirm branch); AppState foreground = override document.visibilityState + dispatch visibilitychange ([appstate](rnw-appstate-foreground-e2e.md)); mixed <Text> is never an exact-text DOM leaf, assert on standalone leaves ([text](rnw-mixed-text-not-dom-leaf.md)); onboarding pager = the scroll-snap div, warmed root goto ([slider](mobile-onboarding-slider-e2e.md)).
- [Blade/Alpine attribute truncation](blade-json-in-double-quoted-attr.md) — @js in double-quoted attrs; [multi-line @json](blade-json-directive-complex-arg.md); [x-data quote](alpine-xdata-attr-quote-truncation.md); [CSS trailing ';'](css-string-emission-trailing-semicolon.md); PHP [] → JS array in @js, cast (object) for keyed maps ([empty](php-empty-array-js-object.md)).
- [Mobile admin-removal lockstep](mobile-admin-removal-lockstep.md) — stripping admin from 1inme-mobile stays typecheck-green but breaks push-action/auth-flow .mjs harnesses, notifications.tsx /admin remap, orphaned admin test:unit entries + the e2e-mobile-pairings validation; grep scripts/ & .replit too.
- [Mobile web keyboard focus ring](mobile-web-focus-ring-pattern.md) — RNW Pressables have no default focus outline; on-brand :focus-visible ring via web-only dataSet marker + injected stylesheet + CSS var, in lockstep across FloatingTabBar + DrawerSidebar; e2e must point APP_URL at a warm expo server (throwaway boot too slow).
- [ScrollReveal visibility gotchas](scroll-reveal-visibility-failsafe.md) — reveals need an unconditional backstop (idempotent applyReveal + 2200ms failsafe) or sections stick at opacity 0; e2e asserts ancestor-walk EFFECTIVE opacity>0 (isVisible() passes at opacity:0), never force reduced-motion ([opacity](e2e-opacity-visibility-check.md), [trap](scrollreveal-reduce-motion-e2e-trap.md)).
- [Admin Block Defaults — searchable font picker](admin-block-defaults-font-picker.md) — replaces free-text font inputs on /admin/block-defaults; reuse `font-picker.blade.php` (hideCustomFonts=true); Alpine bridge for `setStyle` + override badge; preview-frame Google Font loading.
- [claude-training ⇄ api.md anchor guard](claude-training-api-index-guard.md) — §12 `api.md#` cross-links must resolve to github-slugger heading slugs (skips code fences); coverage warn is silent at 0 links, `--strict` to fail. "all 0 resolve" is EXPECTED when the §12 cross-link index isn't merged into the env.
- [AJAX auth e2e — OTP verify success](auth-ajax-e2e-verify-success.md) — good verify must seed an EXISTING user (fresh email stalls single-worker php -S); assert status+navigation not JSON body; [slow-write waits](e2e-editor-create-cold-rds-latency.md) cold RDS POSTs >10s, waitForResponse first ([same-url](same-url-redirect-waitforurl-noop.md)); [find-in-page proxy](native-find-e2e-scrollintoview.md) scrollIntoView a VISIBLE match, browser find bar unreachable.
- [doc-constants drift guard](doc-constants-drift-guard.md) — validates high-stakes constants the 5 docs/ name verbatim still exist in LIVE-parsed PHP source; REVERSE of mobile-docs-parity, keep both; empty parsed set = exit 2.
- [Prod RDS split & seeding](prod-db-split-run-command-override.md) — prod run prelude exports DB_HOST from PROD_DATABASE_URL (host-only; env-scoped vars can't override secrets); seeder-backed marketing pages 404 on prod until their site_pages row is inserted into prod RDS ([seed](prod-site-page-seed-after-publish.md)).
- [AI gate page vs plan allowlists](ai-gate-page-plan-allowlist.md) — voice has 3 gates (master/toggle/plan allowlist); allowlist w/ nonexistent plan slug blocks everyone; debug prod settings on RDS, not the Replit replica.
- [Repeatable-group old() repopulation](form-repeatable-old-input.md) — public form 422 restore needs TWO lockstep surfaces: per-field `old('rep_{secId}.{idx}.{fieldId}')` seed AND the section's Alpine `rCount` seed, else copies beyond index 0 stay hidden (data looks lost).
- [User::created mirrors email/mobile → linked_identifiers](user-created-linked-identifier-observer.md) — a fixture user w/ `mobile` already gets a VERIFIED phone linked-id (email is the single primary); manually inserting a second primary hits `one_primary_per_user` (23505) — surfaces as a Playwright `(0ms)` beforeAll failure; UPDATE the observer's row, never insert.
- [Mobile provider root-scope](mobile-context-provider-root-scope.md) — a context provider feeding BOTH tab screens and non-tab root-stack routes must mount at root, not in (tabs); siblings get the empty default context (workspace create/edit was silently broken).
- [WhatsApp/mobile-only signup accounts](whatsapp-signup-mobile-only-accounts.md) — email-null signup needs nullable users.email; user-count oracle must dedupe users vs linked_identifiers mirror; while-exists slug loops are O(n) queries per signup.
- [Shared password-field partial](password-field-partial-rollout.md) — all password inputs use the eye-toggle partial; never put `@blur` in @include args (Blade compiles it), no-Alpine pages get inline JS toggle.
- [EAS Android APK builds](eas-android-build.md) — EXPO_TOKEN robot under "eefind"; EAS_NO_VCS=1, placeholder projectId blocks init, duplicate appExtensions fatal; [throwaway-Expo harness](expo-throwaway-harness-exit.md) acquireServer API, must process.exit(0) after PASS.
- [Deploy/publish health probes](deploy-health-probe-slow-home.md) — publish GETs each artifact.toml `paths` entry; probe fast `/up`, never heavy `/` ([probe](restart-workflow-heavy-page-probe.md)); [EC2 pipeline lessons](ec2-deploy-pipeline-lessons.md): pre-update checkout, pin pnpm, CI=1, sudo -n needs in-grant command.
- [Bulk export pattern (contacts/general)](bulk-export-pattern.md) — async threshold 500; sync=stream; async=ContactExport row+job+polling page; signed URL (temporarySignedRoute) for mobile/no-auth download.
- [Session driver file vs database](session-driver-file-vs-database.md) — Devices & sessions revoke only works on the database driver; dev/e2e pin file, e2e must boot with SESSION_DRIVER=database (passthrough-listed); prod currently inherits file.
- [storage-url-resolve guard](storage-url-resolve-guard.md) — payload emissions of storage-backed image columns must go through PublicStorageUrl::resolve() (CDN vs slow /storage 302); exceptions go in the guard's ALLOWLIST.
- [better-sqlite3 needs source build on Node 24](better-sqlite3-node24-source-build.md) — no prebuilt binary, background node-gyp rebuild; [Electron CI packaging](electron-desktop-ci-packaging.md) — strip non-Linux binary overrides in CI, explicit .cjs --config, empty WIN_CSC_LINK breaks signing.
- [Bulk copy sweeps via guard file:line output](text-sweep-guard-line-numbers.md) — span-blanking scanners must preserve newlines or line numbers drift and bulk fixers edit the wrong lines.
- [Platform-asset CDN has no CORS](platform-asset-cdn-no-cors.md) — import curated assets into the vault server-side (import-platform-asset + S3 key); sanitizeUrl strips relative /f/ vault paths to '' ([sanitizer](biolink-sanitizer-blanks-relative-vault-urls.md)).
- [Link model has no factory](link-model-no-factory.md) — Link::factory() tests fail at boot; seo_title not meta_title; create via Link::create + workspace binding; redirect links are [type 'url' not 'short'](short-redirect-link-type-url.md) + prod domain_id.
- [Query-columns static guard](query-columns-guard.md) — 3-tier literal-column validation w/ per-file SQL-alias learning; workspaces/follows/vault use owner_user_id/creator_id/created_by_user_id, never user_id.
- [Plan-features raw-read guard](plan-features-read-guard.md) — no raw $plan->features in gating; getPlanFeature()/bypass-check only; check:plan-features-reads ratchet, baselines only shrink.
- [Creator Profile cpLive preview contract](cp-live-preview-contract.md) — density/theme postMessage gated by cp_preview + (owner session OR relative signed URL); post from inside the page (origin check).
- [GitHub CI workflows](github-ci-workflows.md) — PHP 8.4 pin, MySQL leg removed (PG-only migrations), Vite build required before PHPUnit; remaining test failures are pre-existing.
- [Zio Dialer caller-ID overlay](zio-caller-id-overlay.md) — at-ring features must be pure native (JS runtime dead on ring); role grant via OnActivityResult re-check, allow the call before lookup.
- [Block defaults blank content](block-defaults-blank-content.md) — explicit ''/[] overrides are real blanks (array_replace), start_blank blanks non-structural keys, `_placeholder` recomputed; JSON textarea is the ONLY submitted content field.
- [Admin light-mode ak-* classes + ratchet guard](admin-light-mode-ak-classes.md) — shared ak-* helpers in the admin layout (never ak-strong on solid buttons); check:admin-light-mode-text ratchet: new files clean, baselines only shrink ([guard](admin-light-mode-text-guard.md)).
- [Playwright PDF layout vs viewport](playwright-pdf-viewport-fit.md) — measurements feeding pg.pdf() need viewport = physical page px; verify print QRs at ~100dpi renders ([zbar](zbar-decode-render-dpi.md)); booklet imposition detail in [print-booklet-imposition.md](print-booklet-imposition.md).
- [AI builder auto web-photo tier](ai-builder-auto-web-photo-tier.md) — auto search only when preview never reviewed; must check capReached() before search (search() records, never blocks); test needs ≥200px PNG.
- [Runtime config cache vs cleared admin setting](runtime-config-cache-vs-cleared-admin-setting.md) — nulling an admin credential mid-process leaves the boot-time config() copy; clear config() too before asserting unconfigured.
- [Real-API expo-web e2e harness](mobile-real-api-expo-harness.md) — boot own php -S Laravel + tinker-seeded Sanctum token + throwaway Expo w/ baked API base; CDN needs a CORS header shim; SKIP exits 0 so grep the log for PASS.
- [Showcase account is a real user account](showcase-account-is-real-user.md) — showcase:seed WIPES the showcase owner account (see showcase:seed source) on prod; "saves not working" there usually = a reseed reverted her edits.
- [DatabaseSeeder idempotency vs provisioning migrations](databaseseeder-idempotent-vs-migrations.md) — data migrations pre-create roles/admin on fresh migrate; DatabaseSeeder must firstOrCreate (create() dies 23505 on migrate+seed).
- [Template design session draft bridge](template-design-session-draft-bridge.md) — hidden draft biolinks need 3 guards: My Links exclusion, owner-gated public render (editor preview needs owner access), deterministic oldest-draft pick.
- [Chunked card-gallery pattern](chunked-card-gallery-pattern.md) — huge Blade card grids: SSR first chunk + Alpine chunk streaming + light filter index; demo-login is the Gmail demo-login account (see demo-login config), not the legacy 1inme demo address; hoist invariant per-card DB queries out of card loops ([loop](blade-per-card-db-query-loop.md)).
- [Workflow-count bloat wedges env rebuild](workflow-limit-rebuild-lock.md) — 170 registered workflows made every restart time out "waiting for run environment to rebuild"; prune to the core dev servers via removeWorkflow.
- [Design-locked templates](design-locked-templates.md) — fixed-block prefix must be enforced in EVERY position-mutation sink (update/reorder/move, web+API); replace-applies clear the detach release stamp.
- [Image-block hero decorations (_photo_*)](image-block-photo-decorations.md) — decorations live in _style._photo_* so variants carry them; mobile [handle].tsx must consume them (web renderer alone fails parity review); check upstream VERSION before assuming your bump landed.
- [Editor widget e2e gotchas](dropzone-browsetype-accept.md) — dropzone accepts can hide Stock tab (pass browseType), x-for buttons need :text-is CSS; SortableJS preventOnFilter:false ([sortable](sortable-preventonfilter-input-focus.md)); scaled iframe grids use ResizeObserver + poll page.frames() ([iframes](scaled-iframe-card-grid.md)); pill text inside a toggle button must not repeat sibling tab labels or has-text strict-mode breaks ([pill](section-pill-text-selector-collision.md)).
- [Seeder text ownership: no HTML markers](seeder-notes-ownership-no-html-marker.md) — SafeHtml renders HTML comments as visible text; detect seeder-owned notes by content match against curated+legacy snapshots.
- [Biolink full-width block margins](biolink-full-width-block-margins.md) — no container H-padding; side spacing = wrap margin, '0' is real (test !== ''), skipHorizontalMargins at top level.
- [Infinite animations break Playwright actionability](infinite-animation-actionability.md) — shimmer/reveal wrappers never pass the "stable" check; submit via evaluate/requestSubmit; CheckPlanLimit now honors plan_limits.bypass.
- [Unified block background picker](block-bg-unified-picker.md) — bg_color holds color OR gradient; card bg sanitized in sanitizeSettings not _style; bg_attachment=fixed renders on a .bg-page-fixed layer ([fixed](biolink-bg-fixed-layer.md)); mobile preset swatches are pre-rendered PNGs, regenerate on CSS change ([swatches](bg-preset-swatch-thumbnails.md)).
- [Blade inline-script JS parse guard](blade-inline-js-guard.md) — extract+vm-compile inline <script> from Blade; echo placeholder must be an identifier, two compile passes for conditionals, blank @php file-wide first.
- [Cached platform lookups vs workspace scope](cached-platform-lookup-scope-poisoning.md) — Cache::remember over a workspace-scoped model caches EMPTY platform rows inside authed requests; withoutGlobalScopes().
- [Editor forms AJAX-only + previews untracked](editor-forms-ajax-only-preview-untracked.md) — never re-add hidden per-block settings templates; picker lists via EditorPaletteLists only; signed `_preview` renders skip ALL tracking.
- [Quick-shorten sheet alias gate](quick-shorten-sheet-alias-gate.md) — mobile sheet blocks submit on negative alias verdict (two e2e harnesses assert it); ✓/✕ status prefix is client-rendered, don't drop it.
- [Biolink effect bg types](biolink-effect-bg-types.md) — tiles/mesh/pattern/torn lockstep across catalog+controller+editor+renderer+mobile; server stamps bg_effect_colors; mobile previews come from a TS mirror of the PHP catalogs — web-only edits silently fall back to gradient ([mirror](mobile-bg-effect-catalog-mirror.md)).
- [Electron sendInputEvent key codes](electron-sendinputevent-keycodes.md) — needs accelerator codes (Left/Return), DOM key names (ArrowLeft) are silently ignored; Enter/Tab need a char event too.
- [Service Booking staff/self-service](service-booking-staff-selfservice.md) — place() returns an array not a model (8 stale FlowTest failures pre-exist at HEAD); slot opts need explicit capacity; public_token is a uuid column.
