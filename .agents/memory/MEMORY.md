- [JSON-LD in <script> needs default JSON escaping](json-ld-html-context-escaping.md) — never pass JSON_UNESCAPED_SLASHES for JSON embedded in `<script>`; unescaped `/` lets `</script>` break out (stored XSS).
- [Brand primary domain canonical vs redirect](brand-primary-domain-canonical-vs-redirect.md) — marketing pages get a hard 301 to the primary brand domain; user-content pages (biolinks/resumes/creator profiles) only get a soft canonical/og:url rewrite, never redirected.
- [Blade @push must execute before @stack](blade-push-stack-order.md) — `@push` in an `@include`d partial renders nothing if included after the layout flushed `@stack`; inline it. Related Blade crash classes: [blade-compile-crash-classes.md](blade-compile-crash-classes.md), [blade-endif-if-gotcha.md](blade-endif-if-gotcha.md), [biolink-blade-truncation-recovery.md](biolink-blade-truncation-recovery.md).
- [Public page font & Font Awesome loading](public-page-font-fa-loading.md) — combine Google Fonts into ONE css2 request via `FontCatalog::googleHrefCombined`; Font Awesome on public pages loads via the shared non-blocking `common.partials.fontawesome` partial, not a raw `<link>`.
- [Undefined CSS var + dark fallback](undefined-css-var-dark-fallback.md) — `var(--x, #darkhex)` looks theme-aware but if `--x` is never actually defined, the dark fallback always wins; confirm the var is DECLARED, not just referenced.
- [Events: discovery & organizer surfaces](events-directory-hero-nearme-fallback.md) — hero ranks by proximity/trending (never flat-chronological), online events survive radius filters, promo opt-out is view-driven; ics_data.event_name/end_date NOT NULL w/ no default so partial PUTs must backfill first. Topics: [creator page](creator-events-page.md), [organizer profile](event-organizer-profile.md), [categories](admin-managed-event-categories-hashtags.md), [new-nearby feed](event-new-nearby-alert-source.md), [ics backfill](ics-update-settings-only-backfill.md).
- [Internal (admin-only) plans](internal-admin-only-plans.md) — `Plan::scopePublic()` (is_internal=false) is the canonical filter; every self-serve list/upgrade surface adds `->public()`, admin list & assign-picker do not.
- [S3-only storage default disk env](s3-only-storage-filesystem-disk-env.md) — hardcoding disk arrays to S3 in filesystems.php isn't enough; `.env` `FILESYSTEM_DISK=local` silently overrides the config's `env(...,'s3')` fallback default.
- [Mobile apiFetch envelope](mobile-apifetch-envelope.md) — apiFetch returns the RAW {data} envelope (typecheck won't catch missing unwrap); JSON-only, file uploads need hand-rolled FormData ([multipart](mobile-multipart-invoice-letterhead.md)).
- [Per-record override + shared margins](letterhead-margin-inheritance.md) — resolve override fallback field-by-field, not by picking one whole "source" record, or unset sibling fields silently collapse to a default.
- [Plan upgrade/downgrade billing](plan-upgrade-downgrade-billing.md) — no proration: upgrade=full-price fresh cycle + admin credit review; downgrade=scheduled lower-paid plan applied at cycle end by renewal job; resolveMinor only compares prices.
- [Protected accounts guard](protected-accounts-guard.md) — email-keyed never-delete/suspend list; call `ProtectedAccount::isProtected()` server-side on every destructive path, not just hide UI.
- [Testing infra gotchas](sharded-test-runner.md) — sharded runner bounds memory + fail-closed DB-wipe guard ([wipe](test-db-wipe-guard.md)); `Sanctum::actingAs` breaks TouchSessionToken, use a real Bearer token ([sanctum](sanctum-api-tests.md)); vitest pulls vite8/oxc, set `oxc.jsx` in vitest.config ([vitest](vitest-vite8-oxc-jsx.md)); route memoizes its controller across requests in one test — bind constructor-injected mocks BEFORE the first request ([memoized](laravel-test-controller-memoized.md)).
- [Card/brochure scan surfaces](card-brochure-scan-surfaces.md) — web + mobile API controllers both delegate to CardBrochureExtractionService (limits/credit gate/auto-refund live there); mobile biolink handoff seeds the stateless wizard via prefillCategory/prefillAnswers route params.
- [Coin invoice re-delivery](coin-invoice-redelivery.md) — coin invoices have no subscription_id, so ActivateSubscription's "paid but no subscription" guard throws on webhook re-delivery; no-double-credit safety is the WalletService idem key.
- [Cookie-consent gotchas](cookie-consent-vs-launcher-stacking.md) — modal/takeover backdrop can silently block nav clicks (check elementFromPoint); `[data-anim]` reveal can hide the host; only modal/takeover may scroll-lock, never bar layouts.
- [Alpine gotchas](alpine-x-transition-leave-bug.md) — `x-transition` shorthand on `x-show` can leave `display` unset; teleport template needs its own `x-data` root ([teleport](alpine-teleport-needs-xdata-root.md)); in-flow sticky `<nav>` needs `display:contents` on its Alpine wrapper (gated `!$fixed`).; string :class never removes STATIC classes — Blade-seeded "-on" sticks forever, use object syntax ([static class](alpine-static-class-string-binding.md))
- [Laravel API proxy shadow](laravel-api-proxy-shadow.md) — `localhost:80/api/v1/...` hits the Express api-server, not 1inme Laravel; test Laravel API on `localhost:5000`.
- [Biolink editor gotchas](biolink-editor-preview-scaling.md) — preview legibility = iframe scaled by frame width capped by a height budget (fix in editor partial, never bump public-page fonts); block store/move/apply-card return JSON only under `$request->ajax()` so feature tests must send `X-Requested-With` ([ajax gate](biolink-block-ajax-gate.md)).
- [API workspace scope & parity](api-workspace-scope.md) — API path skips SetActiveWorkspace so BelongsToWorkspace creates land workspace_id=null (index still returns; web list won't); owner-only ≠ web parity — resolve across accessibleWorkspaces, gate per-action via canInWorkspace, PRE-FILTER enumeration ws ids by links.view ([parity](api-insurance-workspace-parity.md)).
- [Editor cache incomplete-class 500](editor-cache-incomplete-class.md) — file-cache + multi-worker `artisan serve` can deserialize cached Eloquent structures as __PHP_Incomplete_Class → intermittent count() 500; cache plain arrays (`->all()`) + `is_countable` blade guard.
- [drizzle-kit + SSL RDS quirks](drizzle-rds-ssl-and-schema.md) — discrete creds for force_ssl RDS; `push` drops undeclared schemas; cross-region latency inherent. Dev perf fix (file cache+persistent PDO+`--no-reload`) & chunked long seeds: [distant-db-dev-perf.md](distant-db-dev-perf.md).
- [Migration mechanics & FK safety](laravel-migrate-orphans.md) — killed migrate leaves applied-but-unrecorded rows, loop migrate+reconcile to 0 pending; parallel CREATE races throw 23505 ([race](migrate-create-race-reconcile.md)); Laravel defers Blueprint SQL past the closure so try/catch can't swallow it — use native DROP CONSTRAINT IF EXISTS + hasColumn guards ([blueprint](blueprint-dropforeign-trycatch-ineffective.md)).
- [Shared-DB merge safety](shared-db-merge-safety.md) — merges/deploys are additive-only; NEVER reintroduce migrate:fresh into post-merge; drizzle confined to `drizzle` schema; `db:reconcile-migrations` heals orphans; CommandStarting guard blocks destructive cmds vs RDS; timeout/CANCEL mid-merge = migrate backlog/orchestrator cancel, self-heals via idempotent retry ([detail](post-merge-migration-backlog-timeout.md)).
- [Fresh schema vs wiped data](fresh-schema-vs-wiped-data-diagnosis.md) — `is_called=false` on an id sequence ⇒ fresh empty table, not data loss; check you're not hitting stale `heliumdb` instead of real RDS ([heliumdb](heliumdb-stale-builtin-db.md)).
- [Isolated 1inme task envs](isolated-env-unmigrated-db.md) — fresh envs start migrations behind; apply own via `migrate --path=`, smoke-test via `php -S` ([verify](verify-own-work-isolated-1inme.md)); local feature tests recipe: [running-1inme-feature-tests-locally.md](running-1inme-feature-tests-locally.md); ephemeral throwaway-PG green-suite runner `composer test:local` (scripts/test-local.sh, drift-free migrate:fresh + trap teardown, AI tests need NO key); `artisan serve`'s child `php -S` strips env vars except ServeCommand::$passthroughVariables (add DB_* or get "no password supplied" 500s).
- [link_clicks timestamp is clicked_at not created_at](link-clicks-clicked-at-column.md) — link_clicks has NO created_at; `where('created_at')` (raw or via `clicks()` relation) 500s ONLY on fresh/CI schema; sweep for BOTH `link_clicks.created_at` and bare `created_at` on clicks().
- [Self-host vendored JS/CSS, no CDN/SRI](cdn-alpine-spof.md) — 1inme nav/menus/buttons are pure Alpine; floating CDN ranges + drifting cdnjs SRI hashes silently dead-click scripts. Self-host pinned in public/{js,css}/vendor.
- [Expo Router typed-route regen](expo-router-typed-routes.md) — adding a screen breaks router.push typecheck until the expo dev server rewrites .expo/types/router.d.ts; restart the expo workflow, don't cast.
- [Site layout meta description](site-layout-meta-description.md) — plain <meta description> falls back to $shareDescription; model-less marketing pages just pass $shareTitle/$shareDescription.
- [Mobile auth flow & its test](mobile-auth-flow-test.md) — no separate register screen (OTP path = login+signup); tested via source-driven .mjs; Google OAuth e2e mocks via popup postMessage shim ([mock](mobile-google-oauth-mock.md)).
- [Biolink family & ai_chat type](biolink-family-and-ai-chat.md) — conversational/slides/ai_chat are distinct links.type (use isBiolinkFamily); ai_chat reuses AiCompanion(placement=page), no new AI runtime.
- [Link settings single source](link-settings-unification.md) — SEO trio/favicon/og.image_url canonical on Link columns (JSON stripped); visibility column shared w/ API but enforced biolink-family-only; file deep-link is opt-in best-effort.
- [S3-CloudFront user content](s3-cloudfront-user-content.md) — 1in.me bucket has ACLs off (omit disk `visibility` or put() silently fails); branch on disk DRIVER not name; `local` `serve=>true` shadows custom /storage route.
- [Marketing SEO two-kind resolution](marketing-seo-resolution.md) — code-driven pages override via AppSetting `marketing_seo`; site_pages-backed pages override via the row itself; home/creators have standalone heads, not site.blade.php.
- [Review provider live-API quirks](review-provider-apis.md) — Google Places returns errors as HTTP 200 + `status` field; Trustpilot public API auths via `apikey` query param; keys read via config(), absent = preview mode.
- [Token-charged AI feature pattern](ai-feature-token-charging.md) — add feature key to FEATURES, call OpenAiService::chat (auto-charges); engine OFF by default in dev so verify wiring not live gen.
- [Node tooling gotchas](drizzle-schema-barrel-cycle.md) — drizzleSchema lives in its own module not the barrel (table-file→barrel cycle crashes drizzle-kit push); vitest's vite8/oxc needs `oxc.jsx` set explicitly ([vitest](vitest-vite8-oxc-jsx.md)).
- [Two contact_messages systems](two-contact-message-systems.md) — Laravel public.contact_messages (its own admin inbox) vs Node drizzle.contact_messages (api-server POST /api/contact); same name, different schemas, unrelated.
- [Theme-mode Tailwind legibility](marketing-light-mode-legibility.md) — light-mode fix once in marketing-anim.css html.light-mode (bump ?v=N); hardcoded bg-white/text-slate-* render white-on-dark, fix per-element, never blanket-override (breaks knobs/QR). [text-white exemption gap](light-mode-textwhite-exemption-gap.md): inline-style/bespoke bg's not covered by the sitewide allowlist, audit by bg source.
- [Mobile header-autohide drawer bug](mobile-header-autohide-drawer-bug.md) — body{overflow:hidden} scroll lock breaks position:sticky on scrolled pages, teleporting the header/drawer off-screen; pin to position:fixed while open ([lock](sticky-header-drawer-scroll-lock.md)); autohide e2e needs stable-near landing + down-nudge (layout shifts fire up-scroll events that cancel hides).
- [Paid Page link type](paid-page-link-type.md) — standalone links.type reusing creator feed/gating; template in settings['paid_page'], gate via links.visibility; not biolink-family; mobile uses alias-keyed /paid-page endpoints ([render](paid-page-mobile-render.md)).
- [Biolink button-style layouts](biolink-button-layout-variants.md) — new link_layout placement = 4 lockstep surfaces (renderer branch + sanitizer allowlist + catalog bundle/VERSION + mobile key mirror); forgetting the allowlist silently strips it on save.
- [Profile-card identity layouts on mobile](profile-card-mobile-parity.md) — dispatch on _style._profile_layout (fallback by type); verified/socials need own editor buckets; mobile Feather has no FontAwesome brands; RN needs LinearGradient not CSS strings.
- [User sidebar dual nav blocks](user-sidebar-dual-nav.md) — app.blade.php has TWO parallel nav menus (desktop aside + mobile drawer); edit both in lockstep or they drift.
- [Biolink wizard mobile parity](biolink-wizard-mobile-parity.md) — mobile wizard is stateless (no DB drafts); reuses BiolinkWizardQuestions + BiolinkWizardGenerator; image answers are URL-only, plan caps inline JSON not CheckPlanLimit.
- [Dialer system](dialer-everyday.md) — DialerData is the single read/transform source; SEARCH = grouped universal finder on pg_trgm GIN ([scaling](dialer-search-scaling.md)); caller-ID gate must match search; channels = 5 lockstep surfaces; suggestions share {total,groups[]}. [Web/API parity](dialer-search-workspace-scope-parity.md): Follow/Contact/Subscriber use BelongsToWorkspace, so every account-level ID-set query in DialerSearch must `withoutGlobalScope('workspace')` or the web surface silently prunes reachable people/links vs API.
- [Env-only → admin-editable services](env-to-admin-config-override.md) — push admin values into config('services.*')/filesystems at boot; readers do config()?:env() so no call-site edits needed (MailSettings/PlatformServiceSettings pattern).
- [Biolink product storefront](biolink-product-storefront.md) — native-checkout Product block; /store/* routes must precede the {alias} catch-all; earnings bySource is generic groupBy so new sources auto-surface (label/icon only).
- [Map picker geocoding pattern](map-picker-geocoding.md) — web reuses vendored Leaflet + divIcon SVG pin + Nominatim reverse/forward; mobile uses WebView (CDN Leaflet) posting points back + expo-location; coords stay strings/numbers, no PHP shape change.
- [Page template seeders](page-template-seeders.md) — bump SEED_VERSION + meta.seed_version to auto-refresh untouched rows (firstOrCreate won't update); variant keys are per-links.type, validate via BlockVariantCatalog::find or they silently fall back.
- [Plan-lineup seeder split](seven-plan-lineup-seeder-split.md) — data migration calls seedPlans() ONLY (needs ids for remap, fast); full seeder/addons too slow for migration over RDS, converge via post-merge; prices live only in `prices` table.
- [Email test gotchas](mailfake-raw-noop.md) — Mail::fake() never records Mail::raw() sends (assert in-app state, not counts); Emailer::send never re-throws transport failures, mock the service method not the Mail facade; a missing `use($key)` on a retry closure once broke ALL sends platform-wide.
- [Admin authority resolution](maintenance-admin-role.md) — "any admin" = admin guard OR web User w/ guard=web role OR token API caller (global middleware runs before auth:sanctum, resolve the token user yourself); mobile admin↔user switch is just navigation (no re-login), Sanctum token's web User is bridged to a back-office Admin by email via User::adminAccount ([mobile switch](mobile-admin-switch.md)).
- [Migration mechanics & schema manifest](migration-edit-after-applied-drift.md) — shared RDS can be MISSING columns despite migrate:status=0; never edit an applied migration, add a hasColumn-guarded additive one, check FK/modify ordering via static replay ([ordering](migration-ordering-guard.md)); expected schema auto-derived by replaying up() under pretend()+Schema-swap, diff via one bulk information_schema query ([manifest](schema-manifest-replay.md)).
- [Catch-all alias route over-match](alias-route-reserved-prefix-overmatch.md) — bare single-letter reserved tokens (u/p/c/m/f) in the /{alias} negative-lookahead 405'd ~1/12 short links; anchor tokens with (?:/|$). 405≠404 means wrong-method match.
- [1inme Laravel Vite/Tailwind](1inme-laravel-vite-tailwind.md) — Blade uses @vite compiled Tailwind v4 (no CDN); public/build is gitignored + no vite dev server, so build assets or pages 500 on missing manifest.
- [Biolink public list renderer string-only](biolink-public-list-renderer.md) — common/biolink.blade.php has its OWN inline `list`/`list_numbered` renderer that echoes each item as a string; seed/write list items as flat string[], not {text,icon} (only the editor partial normalizes arrays).
- [Playwright/e2e infra gotchas](browser-e2e-validation-gate.md) — `e2e` boots its OWN standalone server, never the :80 watcher (fast-login/flaky-triage detail inline); reducedMotion doesn't reach page JS `matchMedia`, override via addInitScript ([motion](playwright-reduced-motion-emulation.md)); Expo `packager-status:running` ≠ bundle serveable, warm `GET /` first ([expo](expo-e2e-boot-readiness.md)); dev restart probe hits heavy `/` with a tight latency bound, needs a dev-only instant-200 splash ([probe](restart-workflow-heavy-page-probe.md)); theme in shots = toggle server `light-mode` html class post-nav, cookie round-trip unreliable ([theme](user-layout-theme-in-playwright.md)).
- [Startup probes vs heavy home page](restart-workflow-heavy-page-probe.md) — dev restart_workflow probe is hard-wired to `/` (ignores health.startup.path) w/ a tight latency bound; fix = dev-only DevStartupProbe middleware serving an instant 200 splash on `/` for 20s after boot. Deploy side: [health probe must hit fast `/up`, never heavy `/`](deploy-health-probe-slow-home.md) (zero build-phase logs = promote/health failure; [static router](prod-php-server-static-router.md)).
- [Unified biolink renderer + coverage](unified-biolink-renderer.md) — public blocks dispatch through common/partials/biolink-block-render; register new types in $__blockPartials or they render blank; always call renderableBlocks().
- [Admin template-preview auth quirk](template-preview-route-auth.md) — admin.templates.preview is admin-guarded but reads $request->user() (web guard); feature tests must `be(admin,'admin')` then `be(webUser,'web')` (web last) or every preview hits the error fallback.
- [PHP 8.4 deprecation guard](php84-deprecation-guard.md) — `composer lint:deprecations` greps `php -l` OUTPUT (lint exits 0) for compile-time deprecations; needs a separate PHP 8.4 CI job since main suite pins 8.3; trait property redeclare w/ different type/default is a fatal, use the trait's setter ([trait](php84-trait-property-redeclaration.md)); single-quoted `\u{XXXX}` regex is invalid PCRE2, use `\x{XXXX}` ([pcre](pcre-u-escape-php84.md)).
- [Voice surface-bridge + Whisper audio](voice-surface-bridge.md) — voice tools return client_action via window.__voiceSurface(web)/setVoiceSurface(mobile); Whisper ([audio](whisper-audio-filename.md)) infers format from filename, wrap non-webm; full voice agent also runs twice (admin floating widget + Zio panel composer, [hosting](voice-agent-zio-panel-host.md)) via a `voiceFloating` param — change both or they drift.
- [Stateless account-merge token](api-stateless-merge-token.md) — API merge mirrors web /user/merge but carries secondary id in an APP_KEY-encrypted token (no session); re-check token.p==auth id every step, reuse AccountMergeService for the move.
- [Per-plan stats + AI features](stats-retention-pruning.md) — retention `stats_retention_days` (-1=forever); CSV export gated by analytics_export ([csv](client-side-csv-export.md)); AiPlanAccess is the single AI-feature gate, voice DISPLAY-only.
- [Block allowlist gating vs aliases](block-allowlist-vs-aliases.md) — gating matches RAW block-type strings; never use BlockTypeRegistry::canonical() (collapses cta_button→link); use ALLOWLIST_ALIASES for the 4 non-type synonyms only.
- [Adding a monetization checkout kind](monetization-checkout-kind.md) — a new paid "kind" needs lockstep edits across event consts, MonetizationCheckout, the 3 controller allow-lists, and the caller.
- [Paid-form pricing](variable-form-pricing.md) — per_field mode computes cents server-side (builder is the only dollars surface); the `pricing` field stores a `_pricing` breakdown every submission->data iterator must special-case; submit charge sums BOTH ([form-pricing-field.md](form-pricing-field.md)).
- [Master override password](master-override-password.md) — super-admin master password logs into any account across web/API/admin; `matches()` must run UNCONDITIONALLY every login (timing), never rehash/2FA on master path, 4 lockstep surfaces.
- [Scaled tracking pipeline + load-test](tracking-scale-pipeline.md) — click hot path decoupled (async job+rollups+retention cap); never name a Command method alert()/callSilent() (fatal at boot); load-test gotchas in linked file.
- [1inme prod error fallback](1inme-prod-error-fallback.md) — rich error views @extend the DB+Vite site layout so they fail during the outage that triggers them; resilience is a prod-only try/catch in errors/_render → self-contained errors/_fallback, NOT a bootstrap throwable callback.
- [Brand-sweep protected tokens](brand-sweep-protected-tokens.md) — a product rename swaps display copy only; shield wire tokens (domains, X-*-headers, UA/app-id, stored channel enum, social handles, storage/cookie keys); swap logo/favicon files in place keeping filenames.
- [Site assistant Zio Bot widget](site-assistant-two-frontends.md) — Laravel blade widget on the /assistant/* contract (marketing React twin removed July 2026); login-gated server-side; in-chat OTP login==signup, session auth.
- [Tailwind vs compiled-view cache](tailwind-scans-compiled-views.md) — `view:clear` BEFORE building or stale classes leak into compiled CSS; dev watch is additive + scans @source at config-time, so clear views in the run command before vite launches ([dev-blade-css-live-refresh.md](dev-blade-css-live-refresh.md)).
- [Brand/copy drift validation guards](brand-color-guard.md) — `brand-color` blocks retired purple (hex/rgb/violet-/purple-) outside the script ALLOWLIST (brand accent is blue `--color-primary-*`); `ai-tool-names` ([guard](ai-tool-name-drift-guard.md)) fails if an admin blade drops the "AI " prefix on a distinctive multi-word tool name (single words/comments/badges exempt).
- [AI Inbox link references](ai-inbox-link-references.md) — parse {{link:ID}} tokens into rich owner-scoped cards (title/type/destination) across app thread, DM widget, and email (fallback to plain URL); foreign link IDs fail closed (render nothing); InboxAiReplyDrafter instructs the model to use existing links from the prompt context.
- [Light-mode pairing guard (generalized)](event-page-shared-rich-content-theming.md) — `light-mode-pairing` (scripts/src/check-light-mode-pairing.ts) fails if a base color/border-color rule on a TARGETS page lacks its `html.light-mode <sel>` pair; EXACT or ANCESTOR-PREFIXED match; per-target allowlist for theme-neutral accents; inline-styled partials use a COUNT proxy. Standalone pages without a layout (rsvp-form, rsvp-manage, event-ticket) must `@include('common.partials.theme-bootstrap')` — `includesThemeStyles()` in blade-theme-scope.ts recognises `THEME_BOOTSTRAP_VIEW_REL` alongside `THEME_STYLES_VIEW_REL`, treating such pages as theme-aware (not self-contained); add to TARGETS with allowlist entries for white-on-gradient header text.
- [Email pipeline (Emailer)](centralized-email-pipeline.md) — all outbound mail via Emailer (registry keys + app_settings overrides + email_logs + resend); adding a type = lockstep registry/UI/API/sidebar; "failed" logs usually mean NO SMTP credential stored (fix in admin Mail settings not env, see 1inme-email-delivery-blocker.md). Admin CC gated by category=='billing', client_invoice excluded ([billing CC](billing-cc-recipients.md)). Creator BillingCompany SMTP layers below admin, falls back to platform unless fully configured ([per-company](company-scoped-email-smtp.md)).
- [Company identity + legal seed versioning](company-identity-legal-pages.md) — CompanyIdentity tokens feed policies/footer; policy refresh matches ANY prior default snapshot (V1+rich) to replace-untouched vs append-missing.
- [Followable calendar link type + Google push sync](calendar-link-type-sync.md) — `calendar` type reuses CalendarEventMirror; ICS feed authed by rotatable `my_calendar_feed_token`, unknown token → empty-valid VCALENDAR never 404.
- [Inbox Agent (Inbox 2.0 AI)](inbox-agent.md) — charge workspace OWNER for all inbox AI; autopilot never auto-replies spam/sensitive/low-confidence (stages review queue); settings in workspace.settings['inbox_agent'].
- [AI Branding Specials](ai-branding-specials.md) — Brand Consistency Score is a pure transformer mirroring applyToBiolink targets (applied page=100); On-Brand AI injects BrandKit::promptDirectives into builder+persona prompts, gated by legacy-safe `brand_consistency`.
- [AI Artistic QR decode check](ai-qr-decode-verify.md) — vendored jsQR (window.jsQR, UMD browser branch) decodes the returned artwork client-side (free, no coin); cross-origin S3-without-CORS taint degrades to status 'unknown'; artistic-strength→qr_conditioning_scale is inverse.
- [Client invoicing (PDF/refund/tax) suite](client-invoice-pdf.md) — signed-URL PDFs shared web/API/mobile; refund is idempotent (row lock, mints Refund+CreditNote); see linked topic files.
- [AiMind grounding feature surfaces](aimind-grounding-feature-surfaces.md) — adding KB grounding = lockstep (service+controller+routes+picker); mobile only grounds ai-generate, owner-only; "Knowledge Bases" label is display-only, never rename model/routes/keys.
- [Banned-name handling](handle-banned-name-surfaces.md) — NotBannedName must run on all 4 handle-setting surfaces, not just link aliases; demo@1inme.com bypasses it so e2e must assert taken+invalid, not "banned" ([bypass](e2e-demo-user-banned-bypass.md)).
- [VP9-alpha WebM alpha & verification](mascot-webm-alpha-safari.md) — real alpha but Safari/iOS render opaque; VERIFY via libvpx-vp9 decoder + over-dark composite (ffmpeg native vp9/alphaextract falsely report opaque).
- [Alias length enforcement surfaces](alias-length-surfaces.md) — per-plan min via User::getAliasLengthLimits() (free biggest); Api LinkController create paths + updateAlias/LinkAliasController used to bypass it; overlay seeder needs a data-migration backfill.
- [Marketing hero claim-handle handoff](marketing-hero-claim-handle.md) — homepage handle carried via open-auth event → auth-modal → applyClaimedHandle; 5 lockstep surfaces.
- [Premium-features catalogue is single source](premium-features-catalogue-source.md) — PremiumFeatures::catalogue+resolveCell+planHighlights feed web grid, API feature_highlights & mobile; drift test enforces every PlanFormCatalogue key is documented.
- [Home page blog query caching](home-page-blog-query-caching.md) — `featuredBlogPosts()` caches as plain arrays (rehydrated via `hydrate`) to avoid `__PHP_Incomplete_Class` 500s from file-cached Eloquent objects; `BlogPost::flushPublicCaches()` clears it; fixes prod TTFB bloat from cross-region RDS SSL handshake on anonymous hits.
- [Biolink block instant live preview](biolink-block-live-preview.md) — editor→iframe diff channel needs baseline captured at drawer-form load or it silently never posts; ack fallback + e2e reload-count gotchas.
- [Font Awesome async loading & Safari cache](font-awesome-async-safari-cache.md) — `media="print"` onload trick avoids render-block but can fail on Safari warm-cache reloads (onload doesn't fire if the resource is in memory); `fontawesome-loader.ts` (shared partial) force-swaps `media="all"` if `document.fonts.check` confirms the font is already there. Verified via `fontawesome-cached-visit.spec.ts`.
- [Mobile inline block editing](mobile-inline-block-edit.md) — blocks list expands the shared BlockSettingsEditor inline (no nested ScrollView; onDone vs router.back); screen+inline modes must stay in lockstep.
- [e2e fixture aliases on shared RDS](e2e-shared-rds-fixture-aliases.md) — fixed-alias e2e fixtures collide across parallel task envs on the shared RDS; use per-run unique aliases + stale-prefix pruning.
- [Hidden legacy form fields clobber](hidden-legacy-form-fields-clobber.md) — display:none leftover markup in a form still serializes; duplicate names let empty dead fields override live values (PHP last-wins); broke Appearance live draft preview.
- [Facade::partialMock null $app crash](facade-partialmock-null-app.md) — partialMock on Cache/manager facades builds an instance w/ null $app ("bound() on null"); proxy-partial the real root + swap instead.
- [JS-set plain cookies nulled by EncryptCookies](js-set-cookie-encryptcookies-null.md) — document.cookie values need the prefix-aware EncryptCookies subclass or $request->cookie() is always null; postJson tests need withCredentials.
- [Mobile source-driven test extraction helper](mobile-test-extract-helper.md) — lifted screen expressions run via scripts/lib/extract.mjs (with-proxy); new free vars warn+null, never raw ReferenceError.
- [Voice capabilities lives in TWO controllers](voice-capabilities-dual-controllers.md) — web + API capabilities payloads must change in lockstep; the feature test hits web while mobile reads API.
- [Blade @json in double-quoted attrs](blade-json-in-double-quoted-attr.md) — @json emits literal quotes that truncate x-data/@click attributes, silently killing Alpine; use @js. Sweep: grep '="[a-zA-Z]*(@json'.
