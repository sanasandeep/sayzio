- [Platform default timezone sweep](platform-default-timezone-sweep.md) — centralizing a "personal pref, else platform default" fallback needs a 2nd broad grep sweep (models/controllers/services/commands/views), naive grep misses many.
- [Internal (admin-only) plans](internal-admin-only-plans.md) — `Plan::scopePublic()` (is_internal=false) is the canonical filter; every self-serve list/upgrade surface adds `->public()`, admin list & assign-picker do not.
- [Mobile apiFetch envelope](mobile-apifetch-envelope.md) — apiFetch returns the RAW {data} envelope (request `<{data:X}>`, return res.data; typecheck won't catch it); it's JSON-only, so file uploads (letterhead) need a hand-rolled FormData fetch mirroring voiceAssistant.turn ([multipart](mobile-multipart-invoice-letterhead.md); getToken in @/lib/secure not @/lib/auth).
- [Per-record override + shared margins](letterhead-margin-inheritance.md) — resolve override fallback field-by-field, not by picking one whole "source" record, or unset sibling fields silently collapse to a default.
- [Plan upgrade/downgrade billing](plan-upgrade-downgrade-billing.md) — no proration: upgrade=full-price fresh cycle + admin credit review; downgrade=scheduled lower-paid plan applied at cycle end by renewal job; resolveMinor only compares prices.
- [Protected accounts guard](protected-accounts-guard.md) — email-keyed never-delete/suspend list; call `ProtectedAccount::isProtected()` server-side on every destructive path, not just hide UI.
- [Test running & DB safety](sharded-test-runner.md) — `composer test:sharded` bounds memory via shards (migrate once, skip rest via SHARDED_TEST_SKIP_MIGRATION); fail-closed setUp guard blocks migrate:fresh unless DB is `*_testing`/CI ([wipe guard](test-db-wipe-guard.md)).
- [Sanctum API feature tests](sanctum-api-tests.md) — `Sanctum::actingAs` breaks the TouchSessionToken middleware; auth API tests must use a real Bearer token.
- [Card/brochure scan surfaces](card-brochure-scan-surfaces.md) — web + mobile API controllers both delegate to CardBrochureExtractionService (limits/credit gate/auto-refund live there); mobile biolink handoff seeds the stateless wizard via prefillCategory/prefillAnswers route params.
- [Coin invoice re-delivery](coin-invoice-redelivery.md) — coin invoices have no subscription_id, so ActivateSubscription's "paid but no subscription" guard throws on webhook re-delivery; no-double-credit safety is the WalletService idem key.
- [Cookie-consent gotchas](cookie-consent-vs-launcher-stacking.md) — modal/takeover backdrop can silently block nav clicks (check elementFromPoint); `[data-anim]` reveal can hide the host; only modal/takeover may scroll-lock, never bar layouts.
- [Alpine gotchas](alpine-x-transition-leave-bug.md) — `x-transition` shorthand on `x-show` can leave `display` unset; teleport template needs its own `x-data` root ([teleport](alpine-teleport-needs-xdata-root.md)); in-flow sticky `<nav>` needs `display:contents` on its Alpine wrapper (gated `!$fixed`).
- [Laravel API proxy shadow](laravel-api-proxy-shadow.md) — `localhost:80/api/v1/...` hits the Express api-server, not 1inme Laravel; test Laravel API on `localhost:5000`.
- [Biolink editor preview scaling](biolink-editor-preview-scaling.md) — editor preview legibility = iframe scaled by frame width, capped by a height budget; fix in the editor partial, never bump public-page fonts.
- [Biolink block ajax gate](biolink-block-ajax-gate.md) — block store/move/apply-card return JSON only under `$request->ajax()`; feature tests must send `X-Requested-With`, not just postJson.
- [Sanctum API no workspace binding](api-workspace-scope.md) — API path doesn't run SetActiveWorkspace, so BelongsToWorkspace creates land with workspace_id=null; index still returns them (scope skipped), but web list won't.
- [Stateless API workspace parity](api-insurance-workspace-parity.md) — owner-only scoping isn't web parity; resolve across accessibleWorkspaces + gate per-action via canInWorkspace; enumeration endpoints must PRE-FILTER ws ids by links.view.
- [Editor cache incomplete-class 500](editor-cache-incomplete-class.md) — file-cache + multi-worker `artisan serve` can deserialize cached Eloquent structures as __PHP_Incomplete_Class → intermittent count() 500; cache plain arrays (`->all()`) + `is_countable` blade guard.
- [artisan serve env passthrough](artisan-serve-env-passthrough.md) — `php artisan serve` child php -S strips all env vars except ServeCommand::$passthroughVariables; add DB_* there or get "no password supplied" 500s (parent tinker still works).
- [drizzle-kit + SSL RDS quirks](drizzle-rds-ssl-and-schema.md) — discrete creds for force_ssl RDS; `push` drops undeclared schemas; cross-region latency inherent. Dev perf fix (file cache+persistent PDO+`--no-reload`) & chunked long seeds: [distant-db-dev-perf.md](distant-db-dev-perf.md).
- [Migrate orphans & CREATE-race reconcile](laravel-migrate-orphans.md) — killed migrate leaves applied-but-unrecorded rows; loop migrate+reconcile to 0 pending. Parallel CREATE races throw 23505 not 42P07 ([race](migrate-create-race-reconcile.md)).
- [Shared-DB merge safety](shared-db-merge-safety.md) — merges/deploys are additive-only; NEVER reintroduce migrate:fresh into post-merge; drizzle confined to `drizzle` schema; `db:reconcile-migrations` heals orphans; CommandStarting guard blocks destructive cmds vs RDS.
- [Fresh schema vs wiped data](fresh-schema-vs-wiped-data-diagnosis.md) — `is_called=false` on an id sequence ⇒ fresh empty table, not data loss; pre-guard forked task agents can still reset the shared RDS mid-session; also check you're not hitting stale `heliumdb` (unused leftover; sandbox executeSql hits it NOT the app DB) instead of the real AWS RDS — verify via `db:check-expected-columns` ([heliumdb](heliumdb-stale-builtin-db.md)).
- [Blueprint dropForeign try/catch ineffective](blueprint-dropforeign-trycatch-ineffective.md) — Laravel defers Blueprint SQL past the closure, so try/catch can't swallow it; use native DROP CONSTRAINT IF EXISTS + hasColumn guards for idempotent FK/column migrations.
- [Isolated 1inme task envs](isolated-env-unmigrated-db.md) — fresh envs start migrations behind; apply own via `migrate --path=`, smoke-test via `php -S`, not tinker ([verify](verify-own-work-isolated-1inme.md)); trust CI for Feature tests, local impractical over RDS ([tests](running-1inme-feature-tests-locally.md)).
- [Self-host vendored JS/CSS, no CDN/SRI](cdn-alpine-spof.md) — 1inme nav/menus/buttons are pure Alpine; floating CDN ranges + drifting cdnjs SRI hashes silently dead-click/block scripts. Self-host pinned in public/{js,css}/vendor (Alpine, Leaflet), no integrity attrs. See also cdn-sri-vendored-libs.md.
- [Expo Router typed-route regen](expo-router-typed-routes.md) — adding a screen breaks router.push typecheck until the expo dev server rewrites .expo/types/router.d.ts; restart the expo workflow, don't cast.
- [Site layout meta description](site-layout-meta-description.md) — plain <meta description> falls back to $shareDescription; model-less marketing pages just pass $shareTitle/$shareDescription.
- [Mobile auth flow & its test](mobile-auth-flow-test.md) — no separate register screen (OTP path = login+signup); tested via source-driven .mjs (`pnpm test:auth-flow`); Google OAuth e2e mocks the round-trip via context routes + same-origin popup postMessage shim ([oauth mock](mobile-google-oauth-mock.md)).
- [Biolink family & ai_chat type](biolink-family-and-ai-chat.md) — conversational/slides/ai_chat are distinct links.type (use isBiolinkFamily); ai_chat reuses AiCompanion(placement=page), no new AI runtime; embed-only origin gate means same-origin full-page chat just works.
- [Link settings single source](link-settings-unification.md) — SEO trio/favicon/og.image_url canonical on Link columns (JSON stripped); visibility column shared w/ API but enforced biolink-family-only; file deep-link is opt-in best-effort.
- [S3/CloudFront user content](s3-cloudfront-user-content.md) — 1in.me bucket has ACLs off (omit disk `visibility` or put() silently fails); branch on disk DRIVER not name; `local` `serve=>true` shadows custom /storage route.
- [Marketing SEO two-kind resolution](marketing-seo-resolution.md) — code-driven pages override via AppSetting `marketing_seo`; site_pages-backed pages override via the row itself; home/creators have standalone heads, not site.blade.php.
- [Review provider live-API quirks](review-provider-apis.md) — Google Places returns errors as HTTP 200 + `status` field; Trustpilot public API auths via `apikey` query param; keys read via config(), absent = preview mode.
- [Token-charged AI feature pattern](ai-feature-token-charging.md) — add feature key to FEATURES, call OpenAiService::chat (auto-charges); engine OFF by default in dev so verify wiring not live gen; biolink builder constrains to safe block subset + relative image URLs.
- [Drizzle schema barrel cycle](drizzle-schema-barrel-cycle.md) — drizzleSchema lives in its own module, not the barrel; table-file→barrel cycle crashes drizzle-kit push (typecheck won't catch it).
- [Two contact_messages systems](two-contact-message-systems.md) — Laravel public.contact_messages (its own admin inbox) vs Node drizzle.contact_messages (1inme-com /admin/inbox + api-server); same name, different schemas, unrelated.
- [1inme.com gateway CTAs](marketing-gateway-cta.md) — static marketing site; all auth/pay CTAs via @/config (LOGIN/SIGNUP/PRICING); signup-intent→SIGNUP, plan-buttons→PRICING; copy mirrors Laravel SitePagesContent.
- [Marketing blog ↔ Laravel blog sync](blog-feed-sync.md) — marketing /blog reads DB-driven Laravel blog at runtime via CORS-open /blogs/feed.json + /blogs/feed/{slug}.json (camelCase); no static array.
- [Theme-mode Tailwind legibility](marketing-light-mode-legibility.md) — light-mode fix once in marketing-anim.css html.light-mode block (bump ?v=N); dark-mode hardcoded bg-white/text-slate-* render white-on-dark, fix per-element w/ themed vars, never blanket-override (breaks knobs/QR).
- [Paid Page link type](paid-page-link-type.md) — standalone links.type reusing creator feed/gating; template in settings['paid_page'], gate via links.visibility; not biolink-family; mobile uses alias-keyed /paid-page endpoints ([render](paid-page-mobile-render.md)).
- [Biolink button-style layouts](biolink-button-layout-variants.md) — new link_layout placement = 4 lockstep surfaces (renderer branch + sanitizer allowlist + catalog bundle/VERSION + mobile key mirror); forgetting the allowlist silently strips it on save.
- [Profile-card identity layouts on mobile](profile-card-mobile-parity.md) — dispatch on _style._profile_layout (fallback by type); verified/socials need own editor buckets; mobile Feather has no FontAwesome brands; RN needs LinearGradient not CSS strings.
- [User sidebar dual nav blocks](user-sidebar-dual-nav.md) — app.blade.php has TWO parallel nav menus (desktop aside + mobile drawer); edit both in lockstep or they drift.
- [Biolink wizard mobile parity](biolink-wizard-mobile-parity.md) — mobile wizard is stateless (no DB drafts); reuses BiolinkWizardQuestions + BiolinkWizardGenerator; image answers are URL-only, plan caps inline JSON not CheckPlanLimit.
- [Dialer system](dialer-everyday.md) — DialerData is the single read/transform source; SEARCH = grouped universal finder on pg_trgm GIN ([scaling](dialer-search-scaling.md)); caller-ID gate must match search; channels = 5 lockstep surfaces; suggestions share {total,groups[]}.
- [Env-only → admin-editable services](env-to-admin-config-override.md) — push admin values into config('services.*')/filesystems at boot; readers do config()?:env() so no call-site edits needed (MailSettings/PlatformServiceSettings pattern).
- [Blade compile crash classes](blade-compile-crash-classes.md) — @php(short-form)/nested @json/literal token/attached word@if/concatenated @endif@if (no-space→2nd literal+orphan endif, [blade-endif-if-gotcha.md](blade-endif-if-gotcha.md))/missing @endif all compile to invalid PHP → 500; fix source, lint by compiling+php -l every view.
- [Biolink product storefront](biolink-product-storefront.md) — native-checkout Product block; /store/* routes must precede the {alias} catch-all; earnings bySource is generic groupBy so new sources auto-surface (label/icon only).
- [Map picker geocoding pattern](map-picker-geocoding.md) — web reuses vendored Leaflet + divIcon SVG pin + Nominatim reverse/forward; mobile uses WebView (CDN Leaflet) posting points back + expo-location; coords stay strings/numbers, no PHP shape change.
- [Page template seeders](page-template-seeders.md) — bump SEED_VERSION + meta.seed_version to auto-refresh untouched rows (firstOrCreate won't update); variant keys are per-links.type, validate via BlockVariantCatalog::find or they silently fall back.
- [Plan-lineup seeder split](seven-plan-lineup-seeder-split.md) — data migration calls seedPlans() ONLY (needs ids for remap, fast); full seeder/addons too slow for migration over RDS, converge via post-merge; prices live only in `prices` table.
- [Email test gotchas](mailfake-raw-noop.md) — Mail::fake() never records Mail::raw() sends (assert in-app state, not counts); Emailer::send never re-throws transport failures, mock the service method not the Mail facade; a missing `use($key)` on a retry closure once broke ALL sends platform-wide.
- [Maintenance admin-role concept](maintenance-admin-role.md) — "any admin" = admin guard OR web User w/ guard=web role OR token API caller; global middleware runs before auth:sanctum so resolve the token user yourself.
- [Mobile admin dashboard switch](mobile-admin-switch.md) — mobile admin↔user "switch" is just navigation, no re-login; Sanctum token's web User is bridged to a back-office Admin by email (User::adminAccount), which is the operator's authority for all /api/v1/admin/* gating.
- [Edited-after-applied migration drift](migration-edit-after-applied-drift.md) — shared RDS can be MISSING columns despite migrate:status=0 pending; never edit an applied migration, add a hasColumn-guarded additive one; check FK/modify-to-later-created-table ordering via static replay too ([migration-ordering-guard.md](migration-ordering-guard.md)).
- [Catch-all alias route over-match](alias-route-reserved-prefix-overmatch.md) — bare single-letter reserved tokens (u/p/c/m/f) in the /{alias} negative-lookahead 405'd ~1/12 short links; anchor tokens with (?:/|$). 405≠404 means wrong-method match.
- [Schema manifest by migration replay](schema-manifest-replay.md) — expected DB schema auto-derived by replaying migration up() under pretend()+Schema-swap; diff via one bulk information_schema query, never per-table.
- [1inme Laravel Vite/Tailwind](1inme-laravel-vite-tailwind.md) — Blade uses @vite compiled Tailwind v4 (no CDN); public/build is gitignored + no vite dev server, so build assets or pages 500 on missing manifest.
- [Biolink public list renderer string-only](biolink-public-list-renderer.md) — common/biolink.blade.php has its OWN inline `list`/`list_numbered` renderer that echoes each item as a string; seed/write list items as flat string[], not {text,icon} (only the editor partial normalizes arrays).
- [Post-merge timeout / river CANCEL](post-merge-migration-backlog-timeout.md) — migrate:guarded under fail-CLOSED advisory lock vs shared RDS; timeout/CANCEL = migrate backlog or orchestrator cancel (not a script bug), self-heals via idempotent retry.
- [1inme browser e2e gate + infra](browser-e2e-validation-gate.md) — `e2e` gates a curated spec set; boots its OWN standalone server (port ≠5000), never the :80 dev watcher that kills PHP mid-suite; `page.route` matches by path regex not port; fast-login = wait for demo-login POST + pre-seed onboarded_at + submit via JS ([fast login](1inme-browser-e2e-fast-login.md), [flaky-failure triage](e2e-gate-preexisting-failure-triage.md)).
- [Expo e2e boot readiness](expo-e2e-boot-readiness.md) — Metro `packager-status:running` ≠ bundle serveable; harness must warm `GET /` to a real 200 before Playwright navigates, else ERR_EMPTY_RESPONSE/nav-timeout flake.
- [restart_workflow probe vs heavy cold page](restart-workflow-heavy-page-probe.md) — dev probe is hard-wired to `/` (ignores health.startup.path) w/ a tight latency bound; fix = dev-only DevStartupProbe middleware serving an instant 200 splash on `/` for 20s after boot.
- [Blade truncation recovery from git objects](biolink-blade-truncation-recovery.md) — interrupted platform write can byte-truncate even the committed HEAD blob; recover read-only via zlib from a clean-prefix loose blob (no git); biolink top-level chain ends in catch-all @else → biolink-block-render partial.
- [Unified biolink renderer + coverage](unified-biolink-renderer.md) — public blocks dispatch through common/partials/biolink-block-render; register a type once in $__blockPartials; top-level chain needs an inline branch OR a catch-all @else delegating to the partial table, or blocks render blank; always call renderableBlocks(), never read variant_a/b_snapshot directly ([experiment accessor](biolink-experiment-renderer-accessor.md)).
- [Admin template-preview auth quirk](template-preview-route-auth.md) — admin.templates.preview is admin-guarded but reads $request->user() (web guard); feature tests must `be(admin,'admin')` then `be(webUser,'web')` (web last) or every preview hits the error fallback.
- [PHP 8.4 deprecation guard](php84-deprecation-guard.md) — `composer lint:deprecations` greps `php -l` OUTPUT (lint exits 0) for compile-time deprecations; needs a separate PHP 8.4 CI job since main suite pins 8.3.
- [Voice surface-bridge + Whisper audio](voice-surface-bridge.md) — voice tools return client_action(+deferred navigate_to), surface via window.__voiceSurface(web)/setVoiceSurface singleton(mobile), STT dictation metered+plan-gated; Whisper ([audio](whisper-audio-filename.md)) infers format from filename, wrap non-webm in an UploadedFile w/ right extension.
- [Stateless account-merge token](api-stateless-merge-token.md) — API merge mirrors web /user/merge but carries secondary id in an APP_KEY-encrypted token (no session); re-check token.p==auth id every step, reuse AccountMergeService for the move.
- [Per-plan stats + AI features](stats-retention-pruning.md) — retention `stats_retention_days` (-1=forever, 30 floor); prune missing/any-(-1)→no-op; CSV export gated by analytics_export across link/follower/slide + dashboard + mobile; large exports (5k+ rows) use stream-saver / file-system-access API, dates UTC ISO-8601 ([client-side-csv-export.md](client-side-csv-export.md)). AiPlanAccess is the single gate for 8 AI features; quantity caps + availability bools fall back to global caps/legacy allow-lists when plan key absent; voice DISPLAY-only (runtime still voiceAllowedFor).
- [Block allowlist gating vs aliases](block-allowlist-vs-aliases.md) — gating matches RAW block-type strings; never use BlockTypeRegistry::canonical() (collapses cta_button→link); use ALLOWLIST_ALIASES for the 4 non-type synonyms only.
- [Adding a monetization checkout kind](monetization-checkout-kind.md) — a new paid "kind" needs lockstep edits across event consts, MonetizationCheckout, the 3 controller allow-lists, and the caller; confirm runs outside the request cycle.
- [Paid-form pricing](variable-form-pricing.md) — per_field mode computes cents server-side (builder is the only dollars surface); the `pricing` field stores a `_pricing` breakdown every submission->data iterator must special-case; submit charge sums BOTH ([form-pricing-field.md](form-pricing-field.md)).
- [Master override password](master-override-password.md) — super-admin master password logs into any account across web/API/admin; `matches()` must run UNCONDITIONALLY every login (timing), never rehash/2FA on master path, 4 lockstep surfaces.
- [Scaled tracking pipeline + load-test](tracking-scale-pipeline.md) — click hot path decoupled (async job writes+counter_deltas fold+daily rollups+retention cap), never name a Command method alert()/callSilent() (fatal at boot), partitioned PK/unique MUST include time key; load-test ([gotchas](verify-scale-loadtest.md)) isolated DB is far/high-latency not a real load box, rollup backfills 30d w/o watermark.
- [1inme prod error fallback](1inme-prod-error-fallback.md) — rich error views @extend the DB+Vite site layout so they fail during the outage that triggers them; resilience is a prod-only try/catch in errors/_render → self-contained errors/_fallback, NOT a bootstrap throwable callback.
- [Brand-sweep protected tokens](brand-sweep-protected-tokens.md) — a product rename swaps display copy only; shield wire tokens (domains, X-*-headers, UA/app-id, stored channel enum, social handles, storage/cookie keys); swap logo/favicon files in place keeping filenames.
- [Site assistant two front-ends](site-assistant-two-frontends.md) — "Zio Bot" has a Laravel blade widget + cross-origin marketing React widget sharing one /assistant/* contract, change both in lockstep; login-gated server-side; in-chat OTP login==signup, blade=session/marketing=bearer under a SEPARATE key.
- [Tailwind vs compiled-view cache](tailwind-scans-compiled-views.md) — `view:clear` BEFORE building or stale classes leak into compiled CSS; dev watch is additive + scans @source at config-time, so clear views in the run command before vite launches ([dev-blade-css-live-refresh.md](dev-blade-css-live-refresh.md)).
- [Brand-color guard](brand-color-guard.md) — `brand-color` validation blocks retired purple (hex/rgb/violet-/purple-) in primary UI; intentional palettes live in the script ALLOWLIST, brand accent is blue `--color-primary-*`.
- [AI tool-name drift guard](ai-tool-name-drift-guard.md) — `ai-tool-names` validation fails if an admin blade drops the "AI " prefix on a distinctive multi-word tool name; single entity words deliberately unguarded; comments/count-badges/wire-tokens exempt.
- [ml-auto collapses absolute-only blocks](orbital-hero-layout-gotcha.md) — `mx-auto`/`ml-auto` on a grid item kills stretch; a visual whose children are all absolute collapses to ~0 width. Use definite width + flex wrapper.
- [Centralized email pipeline](centralized-email-pipeline.md) — all outbound mail via Emailer (registry keys + app_settings overrides + email_logs + resend); adding a type = lockstep registry/UI/API/sidebar surfaces.
- [Email delivery blocker](1inme-email-delivery-blocker.md) — "failed" email_logs usually mean NO SMTP credential stored, not code; fix is entering creds in admin Mail settings (store in DB, not env).
- [Company identity + legal seed versioning](company-identity-legal-pages.md) — CompanyIdentity tokens feed policies/footer; policy refresh matches ANY prior default snapshot (V1+rich) to replace-untouched vs append-missing.
- [Heatmap API live parity](heatmap-api-live-parity.md) — web per-link SSE heatmap stream is mirrored on /api/v1 as a pollable lastId-cursor "live" endpoint; first poll seeds last 5min, later polls return id>cursor only.
- [Billing email CC list](billing-cc-recipients.md) — admin CC on billing emails gated by category=='billing' in Emailer::applyBillingCc; client_invoice excluded (creator-economy leak).
- [WhatsApp connect prompts](whatsapp-connect-prompts.md) — 3 web surfaces (post-reg step + dashboard card + weekly cmd) share one gate User::hasWhatsappNumber() & whatsapp.send/verify OTP routes; register-with-mobile auto-verifies so those users self-skip.
- [Embeddable link codes](embeddable-link-codes.md) — anonymous CORS /embed/link/{alias}/{card,iframe,embed.js}; page-types→iframe 302 to short URL, card-types→intent-card; non-public→view-on-site; use $link->type_label accessor not typeLabel().
- [Followable calendar link type + Google push sync](calendar-link-type-sync.md) — `calendar` type reuses CalendarEventMirror keyed on calendar_event_id; accounts route is user.calendar.index not .accounts; aggregated ICS feed authed by rotatable `users.my_calendar_feed_token`, unknown token → empty-valid VCALENDAR (200) never 404.
- [Inbox Agent (Inbox 2.0 AI)](inbox-agent.md) — charge workspace OWNER for all inbox AI; autopilot never auto-replies spam/sensitive/low-confidence (stages review queue); settings in workspace.settings['inbox_agent'].
- [AI Branding Specials](ai-branding-specials.md) — Brand Consistency Score is a pure transformer mirroring applyToBiolink targets (applied page=100); On-Brand AI injects BrandKit::promptDirectives into builder+persona prompts, gated by legacy-safe `brand_consistency`.
- [Per-company SMTP & client email templates](company-scoped-email-smtp.md) — creator BillingCompany overrides layer below admin; transport falls back to platform unless SMTP fully configured; editable keys = existing client-facing registry keys only.
- [AI Artistic QR decode check](ai-qr-decode-verify.md) — vendored jsQR (window.jsQR, UMD browser branch) decodes the returned artwork client-side (free, no coin); cross-origin S3-without-CORS taint degrades to status 'unknown'; artistic-strength→qr_conditioning_scale is inverse.
- [Client invoicing (PDF/refund/tax) suite](client-invoice-pdf.md) — signed-URL PDFs (no session) so web/API/mobile share one route; branding falls back BillingCompany→snapshot→config. Refund via ClientInvoiceService::refund (idem key + row lock, mints Refund+CreditNote via CreditNoteService::issue, never id-keyed, [refund-idempotency.md](refund-idempotency.md), [credit-note-two-mint-paths.md](credit-note-two-mint-paths.md), [MonetizationCheckout::refundEventTicket](MonetizationCheckout::refundEventTicket)); edits recompute per-line tax via applyEdits (InvoiceCalculator), not legacy recalculate. Analytics filters (platform/country/browser) match web via AnalyticsDimensionsService; mobile cache key includes filter-string; large CSV exports (5k+) stream-saver direct-to-disk (mobile file-system-access), dates UTC ISO-8601 ([csv](client-side-csv-export.md)).
- [vitest vite8 oxc JSX](vitest-vite8-oxc-jsx.md) — vitest pulls vite 8 (oxc); default oxc has no jsx cfg so .tsx tests fail import-analysis; set `oxc.jsx` in vitest.config, not plugin-react's ignored esbuild.
- [Voice agent hosting](voice-agent-zio-panel-host.md) — full voice agent runs twice (admin Alpine floating widget + plain-JS in Zio panel composer); `voiceFloating` param decouples window.__voice; change both or they drift.
- [AiMind grounding feature surfaces](aimind-grounding-feature-surfaces.md) — adding KB grounding = lockstep (service+controller+routes+picker); mobile only grounds ai-generate, owner-only; external sources = webhook(inbound)+connector(outbound), secrets reveal-once; "Knowledge Bases" label is display-only, never rename model/routes/keys.
- [Banned-name handling](handle-banned-name-surfaces.md) — NotBannedName must run on all 4 handle-setting surfaces (API register, API profile update, web profile update, claimHandle), not just link aliases; demo@1inme.com holds user.banned_names.bypass so e2e can't assert "banned" state, use taken+invalid instead ([e2e bypass](e2e-demo-user-banned-bypass.md)).
- [VP9-alpha WebM alpha & verification](mascot-webm-alpha-safari.md) — real alpha but Safari/iOS render opaque (canvas corner-pixel probe → transparent still after DOMContentLoaded); to VERIFY use libvpx-vp9 decoder + over-dark composite + alpha_mode=1 (ffmpeg native vp9/alphaextract falsely report opaque) — see vp9-alpha-webm-verification.md.
- [Alias length enforcement surfaces](alias-length-surfaces.md) — per-plan min via User::getAliasLengthLimits() (free biggest); Api LinkController create paths + updateAlias/LinkAliasController used to bypass it; overlay seeder needs a data-migration backfill.
- [Marketing hero claim-handle handoff](marketing-hero-claim-handle.md) — homepage "claim your link" carries a handle via the open-auth event → auth-modal hidden field → AuthController::applyClaimedHandle (graceful, mirrors claimHandle rules); 5 lockstep surfaces.
- [Premium-features catalogue is single source](premium-features-catalogue-source.md) — PremiumFeatures::catalogue+resolveCell+planHighlights feed web grid, API feature_highlights & mobile; drift test enforces every PlanFormCatalogue key is documented.
- [AI one-click apply safety](ai-oneclick-apply-safety.md) — suggestion→one-click-apply must confirm (web confirm()/API 409 confirmation_required) AND fail-fast on missing/ambiguous target id, never fall back to first-owned object.
- [Restaurant menu WhatsApp ordering](restaurant-whatsapp-ordering.md) — optional wa.me link ALWAYS built server-side via WhatsappOrderLink::build; number in menu settings JSON (no migration), order-mode only, additive button on confirm; 6 lockstep web+mobile surfaces.
- [Mobile store.ts namespace collision](mobile-store-ts-collision.md) — `lib/api/store.ts` is the biolink Product-storefront client (buyProduct/checkout/getOwnerOrders); the store_menu link type uses a SEPARATE `lib/api/storeMenu.ts`; never overwrite store.ts.
- [Suggestion apply atomic claim](marketing-suggestion-atomic-apply.md) — concurrent apply-suggestion is serialized by an atomic compare-and-set claim in MarketingSuggestionApplier::claimAndApply, not the controllers' isPending read-then-write.
- [Marketing link-type showcase sync](marketing-link-type-showcase-sync.md) — home extra.link_types + features link-types category + meta_desc + count copy + explainer seeder (Str::slug aliases) are lockstep; reaches live ONLY via a migration (+ explainer seeder in post-merge for /demos).
- [Mobile offline unit gate](mobile-offline-unit-gate.md) — `mobile-unit` gate chains 15 source-driven offline mobile tests via `test:unit`; they rot silently when app source evolves (check source intent before assuming a regression); keep live `icon-fonts` out.
- [Native icon-font CI gate](native-icon-font-gate.md) — web icon gate only proves the WEB bundle; native embeds .ttf as Metro packager assets + useFonts() — `e2e-mobile-icons-native` compiles a real iOS bundle (build.js mechanism, not expo export) + static useFonts preload assert; SKIP-on-boot-fail.
- [Marketing site layout bg + sticky](marketing-site-layout-bg-sticky.md) — public.layouts.site must share home's deep --bg/aurora; in-flow sticky <nav> needs display:contents on its Alpine wrapper (gated !$fixed).
- [AppSetting caching](appsetting-cache-invalidation.md) — get() caches 5min, only put() forgets; raw Eloquent writes serve stale config, use put()/`cache:clear`; cached raw null is a MISS that re-queries every request ([null-miss](laravel-cache-remember-null-miss.md)).
- [Home e2e anchor + reduced-motion](home-anchor-and-reducedmotion-e2e.md) — in-page anchor clicks don't set URL hash (assert scroll pos); reducedMotion emulation flaky headless, assert end-state via expect.poll; section-id changes are lockstep across home.blade.php + REQUIRED_SECTION_IDS + README.
- [App-launch mailing list notify](app-launch-notify.md) — `app_launch_signups` emailed once when ANY store URL first goes live (aggregate empty→live transition + hourly sweep); mirrors FeatureLaunchNotifier's throw_on_failure/no-stamp/retry.
- [Delivery Project](delivery-project-comments.md) — client/buyer↔team comments funnel through DeliveryProjectNotifier (4 surfaces: team web/portal/public-share/API); client emails only when client_email set; email keys must match EmailTemplateRegistry; calendar privacy lives on the Calendar row not the project (public tier=existing is_public; rollup excludes project-tier) ([privacy](delivery-project-calendar-privacy.md)).
- [Coming-soon feature-states & notify](feature-states-coming-soon.md) — resolver: forced>!configured(auto)>ready; integration features need a REAL configured signal, config-independent ones stay null; "Notify me" needs BOTH inline hook AND hourly sweep, idempotent via `notified_at` never delete.
- [Forcing 1inme user-layout theme in Playwright](user-layout-theme-in-playwright.md) — theme = server `light-mode` html class only; toggle that class after nav for dark/light shots, cookie round-trip is unreliable.
- [Consolidated Settings hub](settings-hub-consolidation.md) — /user/settings/{tab} hub: repoint routes in-place keeping names, register in SettingsTabs, any-verb redirects LAST; sidebar dual-nav lockstep; mobile parity = list order in profile.tsx.
- [Contact brand-default lockstep](marketing-contact-content-sync.md) — brand contact defaults must match PHP contactExtraDefault across web blade + marketing + mobile DEFAULT_CONTACT_CONTENT (marketing fetches /api/v1/site/contact directly, not codegen); clients seed state so a failed fetch never blanks/fakes a phone.
- [AI Marketing Strategist report surfaces](marketing-strategist-report-surfaces.md) — analysis blocks render TWICE (service toHtml for PDF/public share + show.blade dashboard); add new sections to both; forecast scenarios||bands, outcome needs a baseline or soft-errors.
- [Marketing favicon sync](marketing-favicon-sync.md) — 1inme-com favicons are copied from artifacts/1inme/public and ?v= is derived from config('app.icon_version'); sync-favicons.mjs + favicon-sync gate enforce it.
- [Google Contacts near-real-time sync](google-contacts-realtime-sync.md) — all sync triggers funnel through throttled GoogleContactsSyncService::syncNow (never syncAccount); CRUD pushes immediately; tombstone retry via shared attemptTombstoneDelete.
- [Standalone dialer sync](dialer-standalone-sync.md) — main app REMOVED dialer/contacts/caller-id; standalone is sole home, manifest syncs shared foundation via check:dialer-sync; marketing mockup (icons + caller-ID face) must stay lockstep across home section + feature page.
- [Check-designs template sync](check-designs-template-sync.md) — templates:check-designs generates previews for every LinkTemplate/Variant combination; it's the only surface that renders all 20+ block types in a single request — if it 500s/whitescreens, one block partial is missing its @elseif or has a fatal PHP-8 compatibility bug.
- [Biolink custom favicon layout](biolink-custom-favicon-layout.md) — favicon lives in site-head, not the renderer; custom favicon needs a per-link check in the layout, not just the editor preview; mobile doesn't support custom icons yet.
- [Tailwind watch-cycle crash-loop guard](tailwind-watch-cycle-crashloop.md) — `--kill-others` only tears down when a child EXITS; a `while true` vite watch loop busy-spinning on instant crashes stays alive & silently serves stale CSS — loop must detect fast-exit crash loops and bail non-zero.
- [Showcase/demo seeder verification](showcase-seeder-verification.md) — reflection-phased harness validates a full-graph seeder that exceeds tool timeouts; analytics backfill needs delete-before-reinsert for idempotency; a plain-user demo account still inherits `user-admin` web role unless separately gated.
- [Demo allowlist drift guard](demo-allowlist-drift-guard.md) — BlockReadonlyDemoWrites two-bucket allowlist (non-persisting vs acknowledged persisting/charging); demo:check-allowlist flags drift by LAST name/URI segment; `App\Models\User` is phantom, canonical is `App\Modules\User\Models\User`.
- [Deploy health probe on slow home page](deploy-health-probe-slow-home.md) — 1inme publish "failed" w/ zero build-phase logs = promote/health failure; startup probe must hit fast `/up`, never heavy `/`. Prod `php -S` needs a static router or images/CSS serve as HTML: [prod-php-server-static-router.md](prod-php-server-static-router.md).
- [Deploy-seed heavy demo content](deploy-seed-shared-rds.md) — dev==prod share far RDS so a 15-25min seeder only finishes on the co-located deploy server; run via env=production-guarded migration that BACKGROUND-spawns db:seed, advisory-locked vs concurrent boots; NEVER edit production.run, NEVER `migrate` in dev.
- [Demo login prod + shared demo account](demo-login-prod-and-e2e-writable.md) — demo user/admin logins are public passwordless POSTs; owner enabled BOTH in prod. demo@1inme.com MUST stay writable or the browser e2e gate's write specs break; a safe public read-only demo needs a SEPARATE flagged account.
- [Dashboard widget catalog mobile parity](dashboard-widget-catalog-mobile-parity.md) — when a web widget-catalog feature predates mobile home's fixed layout, mobile parity = a settings-screen control surface, not reshaping mobile home rendering.
- [Competitor Biolink Teardown](competitor-biolink-teardown.md) — fetch-before-charge ordering + refund idempotency; "build better version" reuses AiBiolinkBuilderService's own charge/gate, no new one; guard lets you verify wiring w/o a live OpenAI key.
- [Mobile block editor missing fields](mobile-block-editor-missing-fields.md) — a block type absent from `BLOCK_KINDS` (blocks.ts) renders ZERO editor fields on mobile even if the public renderer supports it; check both the entry AND any bespoke `is<Type>` JSX section, not just the type string.
- [Mobile/docs parity guard](mobile-docs-parity-guard.md) — `parity:check-mobile-docs` baseline+`--accept` guard flags new web block/link types lacking a mobile BLOCK_KINDS entry or docs mention; mobile regressions/new/stale hard-fail, docs info-only; 5 lockstep surfaces.
- [Social connections reuse](reusable-social-autofill-picker.md) — autofill picker = one blade partial + injectable onSelect JS across Alpine-array/flat-DOM forms, uses ALL connections not just searchable; caller-ID merge + dialer Social group + summary must share one scope + reachability helper, never recompute visibility ([searchable fan-out](social-search-caller-id-sync.md)).
- [Event ticketing on ics link type](event-ticketing-ics.md) — paid tiers/QR/check-in + owner ticket refund (MonetizationCheckout::refundEventTicket: DB-lock double-refund guard, gateway reversal outside lock, frees seat, emails attendee via ticketing.refunded) on the `ics`/`calendar` type across web+API+mobile; owner check-in/refund routes live with per-link actions not `/events/*`; live door progress (EventCheckinProgress, web+API+mobile 5s poll) counts by summed ticket `quantity` not row-count; regen expo router types via offline `expo start` when no dev workflow. Cover/gallery/info/interest sections only render via handleEventTicketingPage/rsvpForm paths; a plain ics link with neither flag still just downloads .ics, by design.
- [Absolute-positioned line confined to auto grid column](brand-connector-line-full-span.md) — an absolutely-positioned decorative line only spans its own (possibly narrow, auto-width) flex/grid item box, not the visual gap created by sibling margins; span it from a full-width ancestor instead.
- [Free event RSVP has no JSON API](event-rsvp-webview-no-json-api.md) — embed the existing session/CSRF web form in a WebView, detect success via redirect URL, instead of duplicating RSVP business rules client-side.
- [event.new_nearby is server-side](event-new-nearby-alert-source.md) — "new event near you" alerts are already preference+proximity gated server-side and exposed via GET /notifications; consume that feed, don't reimplement client-side.
- [Event check-in workspace-aware access](event-checkin-workspace-access.md) — 3 lockstep auth call paths (web middleware, unauth QR lookup, stateless API findLink/canAct); RSVP tickets are tier-less EventTickets keyed by rsvp_id, synced via RsvpTicketService.
- [Event tier capacity alerts](event-tier-capacity-alerts.md) — 90%/sold-out owner alert fires from issueEventTicket via EventTierCapacityAlerter; idempotent via 2 stamp cols claimed atomically, reset on capacity raise (web+API lockstep), counts-only no PII.
- [1inme duplicate dev workflow](1inme-duplicate-dev-workflow.md) — canonical 1inme dev server is `artifacts/1inme: web` (artifact.toml); a hand-added duplicate `1INME` workflow reran the same command → port-5000 collision + "artifact failed to run". Remove the dup, restart the canonical.
- [bento-stage clobbers fixed overlays](bento-stage-fixed-overlay-clobber.md) — a fixed-position modal nested as a direct child of `.bento-stage` silently becomes `position:relative` via a CSS specificity tie-break; JS/Alpine state stays correct, only getComputedStyle reveals it.
- [SitePages marketing page addition](sitepages-marketing-page-addition.md) — new landing page is 6 lockstep surfaces (richDefaults, route+catch-all exclusion, controller branch, home teaser+chip+tests, nav, backfill migration); skipping the migration 404s on prod.
- [Plan-feature registries are separate systems](plan-feature-registries-are-three-separate-systems.md) — quantityLimits/featureFlags/aiSuite is only ONE registry; check modules()/moduleKeys()/aiCoinMultipliers() + all features[...] read shapes before calling a key "missing".
